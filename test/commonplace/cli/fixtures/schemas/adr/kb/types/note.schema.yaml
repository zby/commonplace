$schema: "https://json-schema.org/draft/2020-12/schema"
allOf:
  - $ref: "./note-base.schema.yaml"
  - type: object
    properties:
      frontmatter:
        type: object
        properties:
          status:
            enum:
              - seedling
              - current
              - speculative
              - outdated
        additionalProperties: true
