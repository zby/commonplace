=== SEMANTIC REVIEW: codification.md ===

Claims identified: 18

WARN:
- [Completeness] The note defines codification by three criteria: (1) the medium changes (natural language to code), (2) the consumer changes (LLM to interpreter/runtime), (3) verification becomes exact. Structured output schemas (e.g., JSON Schema) satisfy all three: they are written in a formal language, consumed by a validator (not an LLM), and verification is deterministic (valid/invalid). Yet the constraining note's verifiability gradient table places structured output schemas in a middle grade, not at the codification end. The codification note does not address this boundary case — its definition admits more instances than its examples suggest. Either the definition is too permissive (and should add a criterion that distinguishes schemas from code) or schemas are a form of codification the note should acknowledge.
- [Completeness] The note claims "Codification leaves natural language for a symbolic medium entirely." Configuration files (YAML, TOML, HCL) are symbolic, consumed by parsers rather than LLMs, and validated deterministically. They cross the medium boundary but are not "executable code" in the usual sense. The note's definition (medium changes, consumer changes, verification becomes exact) covers them, but the examples and framing all point to executable code. This creates ambiguity about whether declarative-but-formal artifacts count as codification or as a distinct constraining grade.

INFO:
- [Completeness] The note's three examples of codification (slug generator, CSV statistics, frontmatter checks) all share a common shape: replacing LLM computation with a deterministic library or script. A different shape of codification — writing a type system or interface definition that constrains agent output — is absent. Type annotations change the consumer (LLM to type checker), change verification (stochastic to deterministic), and change the medium (natural language to type language). Whether this is codification or a separate constraining grade is unstated.
- [Completeness] The claim "Operations with weak oracles (correctness requires judgment) resist codification" is stated as a binary. The oracle-strength-spectrum note describes a five-level gradient (hard / soft / interactive / delayed / no oracle). Soft oracles — proxy scores that correlate with correctness — partially resist codification but can sometimes be hardened enough to codify (e.g., a heuristic check that becomes a linter rule). The codification note's hard/weak framing compresses this gradient into two bins, losing the middle ground where partial codification is possible.
- [Grounding] The link to deploy-time learning says "the verifiability gradient across which codification sits at the far end." The deploy-time note does confirm this framing, but the verifiability gradient is about four grades of hardening (restructured prompts, structured output schemas, prompt tests, deterministic modules), not specifically about codification's medium-boundary crossing. The codification note uses the link to ground its "far end" positioning, but the deploy-time note's gradient is about verification sharpness, which is related but not identical to the medium-change definition the codification note uses.
- [Internal consistency] The note says "One-shot codification — directly translating a spec into code with no larger body of reasoning behind it — is pure constraining without distillation." This is internally consistent, but the phrase "no larger body of reasoning" is ambiguous. Any spec-to-code translation involves understanding the spec, which draws on knowledge. The distinction between "accumulated methodology" (distillation) and "understanding the spec" (pure constraining) is clear in the extreme cases (the three examples) but blurry in practice. The note does not acknowledge this gradient.

PASS:
- [Grounding] The note's central claim — codification is the far end of the constraining spectrum — is confirmed verbatim by the constraining note: "The last step — codification — is the far end of the spectrum where the medium itself changes (natural language to executable code)." No vocabulary mismatch, no scope mismatch.
- [Grounding] The claim that "Oracle strength determines what can codify" aligns with the oracle-strength-spectrum note, which discusses hard oracles as enabling codification and places codification at the hard-oracle end. The oracle note uses broader language ("what can be automated") but codification is explicitly discussed as the relevant operation.
- [Grounding] The relationship to distillation — "Codification can also be distillation" — is confirmed by the distillation note: "codification... sometimes follows distillation (extract a procedure, then codify it to code)." The orthogonality framing (constraining and distillation as independent dimensions) is consistent across both notes and the shared 2x2 table.
- [Grounding] The constrain/relax cycle as safety valve is confirmed by the constraining note's "Relaxing" section, which describes relaxing as the reverse operation and the constrain/relax cycle as a learning cycle.
- [Internal consistency] The note's definition (three criteria: medium, consumer, verification) is used consistently throughout. The opening paragraph, the second paragraph's restatement, the "When to codify" section, and the "Not codification" examples all cohere with the same definition. No definition drift detected.
- [Internal consistency] The description field ("codification is constraining that crosses a medium boundary from natural language to a symbolic medium (code), where the consumer changes (LLM to interpreter) and verification becomes exact") faithfully represents the body. No claims in the description are absent from or contradicted by the body text.

Overall: 2 warnings, 4 info
===
