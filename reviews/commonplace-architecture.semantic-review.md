=== SEMANTIC REVIEW: commonplace-architecture.md ===

Claims identified: 14

WARN:
- [completeness] The "What's missing" table lists CLAUDE.md as "Missing" with status "Missing," but CLAUDE.md exists at the repo root and is actively used (it is the control-plane router loaded every session). This is a factual inaccuracy — the table reflects an earlier state that has since been resolved but the note was never updated. The table also lists `.claude/skills/` as missing; that directory does not exist either, but unlike CLAUDE.md, its absence may still be intentional or unresolved.

- [completeness] The "Current layout" tree diagram omits several directories and files that exist in the actual repo. Specifically: (1) `kb/notes/research/` is listed in the tree but `kb/notes/meta/` is listed yet does not exist; (2) `kb/instructions/` contains files beyond "connect, convert, ingest, snapshot-web, validate" — the annotation in the tree lists five skills, but the actual directory contains at least ten files (semantic-review, prose-review, prose-review-sweep, semantic-review-sweep, revise-note, re-ingest, maintain-curated-indexes, evaluate-log-entry-for-note-creation, write-instruction, WRITING.md); (3) the `reviews/` directory is absent from the tree despite existing in the repo. The tree is described as the "Current layout" without any caveat that it is approximate or selective.

- [grounding-alignment] The note says "The installation architecture spec calls for `kb/types/` to hold global types" and links to `commonplace-installation-architecture.md`. However, that linked note places global types at `types/` (repo root), NOT `kb/types/`. The installation architecture note's tree diagrams consistently show `types/` at the repo root (both for the commonplace repo itself and for installed projects). The architecture note's claim that "the installation architecture spec calls for `kb/types/`" misattributes the location, which undermines the subsequent argument for why `kb/types/` should be dropped — the source never proposed `kb/types/` in the first place; it proposed root-level `types/`.

INFO:
- [completeness-boundary] The note distinguishes two kinds of types: "Collection types" (concrete structural templates) and "Global types" (maturity ladder policy rules). Boundary case: the `index` type in `kb/notes/types/index.md` is a collection type that applies across multiple directories — indexes can appear in `kb/notes/`, `kb/notes/related-systems/`, `kb/sources/`, etc. This sits between the two categories: it is structurally a template (like collection types) but its applicability is cross-directory (like global types). The note's binary does not account for collection types that have cross-directory reach.

- [completeness-boundary] The layout tree shows `kb/tasks/` with subdirectories `backlog/`, `active/`, `completed/`, but the note does not mention `kb/work/` (the workshop layer). The installation architecture note explicitly includes `kb/work/` in both the commonplace repo layout and installed project layout. This is a gap in the "Current layout" section — it omits a directory that the linked architecture spec considers part of the standard layout.

- [internal-consistency] The note's title is "Commonplace architecture" but the description says it covers "the commonplace repo's own internal layout — what exists, what's missing." The body opens by distinguishing this note from the installation architecture note, saying this one "covers the repo's own layout." But the "Global types belong in CLAUDE.md" section makes a decision that explicitly applies to the installation architecture: "Update the installation architecture spec to match." The note blurs the boundary between describing the repo's current state and making prescriptive decisions that affect the installation spec. This is not a contradiction, but the scope is wider than the description and introduction claim.

- [grounding-alignment] The note cites `directory-scoped-types-are-cheaper-than-global-types.md` as "foundation: why collection-level types/ directories work but a global types/ directory is overhead." The linked note's argument is about context-window costs of loading global type definitions and the absence of an import mechanism in LLM contexts. The architecture note's argument for putting global types in CLAUDE.md is different — it is about hop count and the triviality of the two-sentence rule. These are compatible but distinct arguments: one is about cognitive/context cost, the other is about navigation cost. The link relationship "foundation" slightly overstates alignment; "informs" would be more precise.

- [internal-consistency] The "What's missing" table and the "Global types belong in CLAUDE.md" section create a mild temporal tension. The table presents a snapshot of what is missing (past/present state), while the "Global types" section makes a forward-looking decision. The note does not clearly separate observation from decision, and the table's status markers ("Missing") are ambiguous — do they mean "missing and should exist" or "missing and we decided not to create it"? For `kb/types/`, the note later decides it should NOT exist, which contradicts the table's implication that it is a gap.

PASS:
- [internal-consistency] The core argument — that global types are policy rules thin enough to live in CLAUDE.md while collection types are structural templates that justify their own files — is internally consistent. The distinction between "two-sentence rule" and "multi-section scaffold" is clear and not contradicted elsewhere in the note.
- [grounding-alignment] The link to `instruction-specificity-should-match-loading-frequency.md` with relationship "constrains: what goes in CLAUDE.md vs what the agent reads on demand" is accurate. That note establishes a loading hierarchy where always-loaded surfaces should be slim and task-specific detail loads on demand. The architecture note's decision to put global types in CLAUDE.md (always-loaded, thin) and keep collection types in per-directory files (loaded on demand) directly follows this principle.
- [grounding-alignment] The link to `commonplace-installation-architecture.md` with relationship "the two-tree design for installed projects; this note covers the repo's own layout" is directionally accurate. The installation architecture note does describe a two-tree design, and this note does focus on the repo's own layout. (The `kb/types/` misattribution noted above is a specific factual error within a broadly accurate framing.)
- [internal-consistency] The note's two open questions ("Should kb/instructions/ include an install script?" and "What goes in CLAUDE.md for the repo itself vs installed projects?") are consistent with the note's seedling status and do not contradict any claims made in the body.

Overall: 3 warnings, 4 info
===
