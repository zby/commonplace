=== PROSE REVIEW: operational-signals-that-a-component-is-a-relaxing-candidate.md ===

Checks applied: 8

WARN:
- [Proportion mismatch] The first signal (paraphrase brittleness) is roughly three times longer than any other signal, driven by extended discussion of Rabanser et al. and Ma et al. The core claim of that signal is established in the first two sentences; the remaining ~120 words are source elaboration ("psychologically grounded emotion/personality templates at three calibrated perturbation distances (d=0.1 light lexical, d=0.2 moderate style, d=0.3 substantial transformation) across 14,760 variants and 14 models"). Meanwhile, signals like "process constraints rather than outcome constraints" and "hard to specify failure conditions" -- which are arguably more novel contributions of this note -- get ~50 words each. The proportions weight evidence-citing over the note's own analytical contribution.
  Recommendation: Move the detailed Rabanser/Ma discussion to a shorter inline citation ("Rabanser et al. operationalise a related signal as R_prompt; Ma et al. confirm it across controlled perturbation distances") and develop the thinner signals with examples or testable criteria. Alternatively, extract the detailed source discussion into the Relevant Notes section where it already appears.

- [Source residue] The note's title and framing claim generality about "components" in systems, but the paraphrase brittleness signal is developed entirely through LLM-specific evidence (prompt robustness, emotion/personality templates, model comparisons). Terms like "R_prompt," "Qwen-1.5B highest AUC-E," "14 models" assume the reader is working with LLMs. The other five signals are written in domain-neutral language ("component," "unit tests," "integration"), making the first signal's LLM-specificity a contrast. If the note is about components generally, the LLM evidence is an example that should be framed as one; if the note is about LLM agent components specifically, the title and description should say so.
  Recommendation: Either frame the LLM evidence explicitly as an example domain ("In LLM agent systems, for instance, Rabanser et al. ...") or narrow the note's claimed scope to LLM/agent components where the evidence directly applies.

INFO:
- [Unbridged cross-domain evidence] The interpretation sentence "we interpret the findings as evidence that paraphrase brittleness is a measurable and prevalent signal -- exactly what you'd expect if many agent components encode theories about input format rather than specifications of meaning" bridges from the cited LLM studies to the note's theoretical framework. The bridge is present but thin -- it asserts the connection ("exactly what you'd expect") without explaining the shared mechanism that makes LLM prompt sensitivity an instance of the general theory/specification distinction. The note would be stronger if the bridge stated why prompt brittleness in LLMs shares a mechanism with the vision-features pattern (both cases: the component was optimized for a surface representation of the input rather than the underlying structure the input represents).
- [Confidence miscalibration] The opening paragraph appropriately hedges: "they detect badly-fitting theories, not the theory/spec distinction itself, since a well-regularised learned component can pass all five early signals yet still encode a theory that scale will supersede." Individual signal descriptions, however, use assertive causal language: "it's relying on surface patterns rather than capturing the underlying structure," "the spec is probably a theory." The tension is minor because the framing paragraph sets the right epistemic context, but a reader encountering a signal in isolation (via search) would get the assertive version without the hedge.

CLEAN:
- [Pseudo-formalism] No mathematical notation, variables, or equation-like structures. The perturbation distances (d=0.1, d=0.2, d=0.3) are quoted from Ma et al., not the note's own apparatus; they describe the source's methodology, not a formal model the note introduces.
- [Orphan references] All specific numbers and named findings are cited with linked sources. "14,760 variants and 14 models" traces to the Ma et al. source. "R_prompt" traces to Rabanser et al. "Qwen-1.5B highest AUC-E" traces to Ma et al. No uncited empirical claims found.
- [Redundant restatement] The "Composition failure" signal could appear redundant with the "Isolation-vs-integration gap" signal, but the note explicitly distinguishes them: isolation-vs-integration is an early-detection signal testable before production, while composition failure is a "late-stage confirmation" that is "expensive to discover." The distinction is load-bearing and not restatement.
- [Anthropomorphic framing] No language attributing mental states to models or components. "Relying on surface patterns rather than capturing the underlying structure" describes functional behavior of a component, not cognitive states. "It has overfit to a particular theory" is metaphorical but standard technical usage.

Overall: 2 warnings, 2 info
===
