$schema: "https://json-schema.org/draft/2020-12/schema"
allOf:
  - $ref: "../../types/note.schema.yaml"
  - type: object
    properties:
      frontmatter:
        type: object
        required:
          - description
          - type
          - last-checked
        properties:
          type:
            const: kb/agent-memory-systems/types/lightweight-review.md
          last-checked:
            type: string
            format: date
        additionalProperties: true
      headings:
        description: "Required lightweight-review headings — the same comparison elements as agent-memory-system-review, but doc-grounded and claim-level. Borrowable Ideas nests as a ### subsection under ## Comparison with Our System. ## Trace-derived learning placement and ## Read-back placement are optional, each used only when the sources document the corresponding qualifying mechanism."
        type: array
        allOf:
          - contains:
              const: "## Core Ideas"
          - contains:
              const: "## Comparison with Our System"
          - contains:
              const: "## Curiosity Pass"
          - contains:
              const: "## What to Watch"
  - if:
      properties:
        frontmatter:
          required:
            - tags
          properties:
            tags:
              contains:
                const: trace-derived
    then:
      properties:
        body:
          pattern: "Trace-derived learning placement"
  - if:
      properties:
        frontmatter:
          required:
            - tags
          properties:
            tags:
              contains:
                const: push-activation
    then:
      properties:
        body:
          pattern: "Read-back placement"
