Verdict: BLOCK

Checked: all 31 audit resolutions against `candidate.md` (30 realized; F14 realized in the body but not in the footer, below); disposition drift (C1 central; C2–C8, C13–C15 as support; C10/C12/C13/C14 cited; C9, C11, and the omit-list absent — no new commitment found); every inline and footer link against its target note's text and H1 (all 15 targets resolve from `kb/notes/`, no `kb/sources/` dependency, examples generic); contract (one central proposition; title composes; universal mode declared in the thesis with two non-empty refuters in the closing paragraph; statistical clause carries its refuter; frontmatter exactly `description`, `type`, `traits`, `tags`; `## Scope` and `## Open Questions` present; footer labels `extends`, `grounds`, `grounds`, `extends`, `contrasts`, `mechanism` all authorized for notes; no `user-verified`); prose (no workshop markers, no stock phrases, absolutes only where the disposition commits them); and the packet residual (answered as mechanism in paragraph 4 and the thesis, with the trial objection published as Open Question 2). Three anchored blockers remain, each a one-sentence fix.

## Blockers

1. Footer, `current-task-fit` edge: "mechanism: how coupled consumers turn external binding into revision cost, and what prices binding chosen before acceptance". The target note states "This note does not supply that decision threshold" and lists the coordination rule as an open question; it names the terms to weigh, not a price. F14 removed exactly this attribution from paragraph 6 ("weighed as the current-task-fit note weighs entrenchment, which leaves the threshold open"), so the footer now promises what the body says the target does not supply, and a reader following the edge to find the price will not find one. Fix: replace the second clause with what the target does supply, e.g. "and the warrants under which binding chosen before acceptance is weighed, with the threshold left open".

2. Paragraph 5: "Form does determine which checks are available and what each use costs in interpretation, as [Theory warrant should be tracked at the finest granularity evidence licenses](…) states: a natural-language premise is revisable without a build step but is reinterpreted at each use. It does not determine what a revision costs". Two faults in one sentence pair. (a) The colon places the gloss under "as [title] states", but the theory-warrant note says only that form "changes which checks are available"; "without a build step" is the pre-formal note's wording and "reinterpreted at each use" is the codification and representational-form definitions', so the link attributes to its target content the target does not carry — the misfit F11 was meant to close. (b) After that gloss, "It does not determine what a revision costs" has "a natural-language premise" as its nearest subject, a competing referent for the intended "form"; the brief asks that no such ambiguity survive. Fix: close the attribution at the source's claim and name the subject, e.g. "Form does determine which checks are available, as [title] states, and what each use costs in interpretation: a natural-language premise is revisable without a build step but is reinterpreted at each use. Form does not determine what a revision costs; …" (keeping the gloss outside the "states" clause, or dropping it).

3. Paragraph 4: "a theory retained with the rationale that produced it can be repaired part by part". The selective-revision note defines the rationale as the record of "why it held the theory" and its Scope says "A recorded rationale is a claim about the theory, not a transcript of how it was formed. Nothing here assumes a system can introspect its own basis." "The rationale that produced it" asserts the formation reading the target explicitly disclaims, and the `grounds` edge invites the reader to verify it there. Fix: "a theory retained with a faithful rationale" or "with the rationale it rests on".
