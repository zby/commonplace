# Freshness JSON contracts

The promoted reference page was retired on 2026-08-24. Semantic boundaries
remain in [freshness architecture](../../reference/freshness-architecture.md);
exact JSON behavior belongs to `src/commonplace/freshness/status.py`,
`src/commonplace/cli/freshness_{ack,retire}.py`, and
`src/commonplace/freshness/transitions.py`.
