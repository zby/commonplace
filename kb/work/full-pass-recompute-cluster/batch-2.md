# Batch 2 — dependents

Run [run-full-improvement-pass-on-note.md](../../instructions/run-full-improvement-pass-on-note.md)
once per note, one note at a time. Read the
[workshop README](./README.md) for operating constraints before starting.

**Do not start until [batch 1](./batch-1.md) is complete**, including any
reframe follow-up it triggers. Both notes here cite batch 1, so a batch-1
reframe moves their premises before their own pass would read them.

Status: blocked on batch 1.

## Notes

| # | Path | Status |
|---|---|---|
| 5 | `kb/notes/a-theory-may-name-a-choice-only-as-a-bound-variable.md` | blocked |
| 6 | `kb/notes/addressability-grain-not-compression-ratio-decides-whether-a.md` | blocked |

## Inbound dependencies on batch 1

- `addressability-grain` **extends** `documentation-generates`, **contrasts**
  `human-recompute`, and **grounds** on `llm-recompute` — three of batch 1's
  four.
- `a-theory-may-name` **contrasts** `superseded-choices`.

Recheck these edges before each pass: if batch 1 reframed a target, the citing
sentence here may state a claim its target no longer makes.

## Per-note context

**5. `a-theory-may-name-a-choice-only-as-a-bound-variable`** — two authors. The
initial write was revised by a second agent, which replaced a section and
dropped a ranking of the two repairs in favour of a selection rule. Its
empirical section restates sweep evidence rather than citing it, because the
notes contract forbids outbound links into `kb/work/` and the source workshop
has since been deleted; that restatement is deliberate and should survive the
pass. This note also states the rule the pass's own collection-fit check
applies, so a finding against it is worth extra scrutiny either way.

**6. `addressability-grain`** — newest of the six, written last. Its claim is
scoped to the recoverable-cache partition, which is what keeps the "only when"
universal rather than hedged; a pass that widens the scope would break that.
Its central evidence is a pair of instances running opposite ways under one
rule, so a compression finding that trims one instance would remove the
discrimination the note rests on. Cites Commonplace as an existential witness.
