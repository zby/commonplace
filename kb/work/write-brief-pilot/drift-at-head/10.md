# Recoverability — target 10

Target: `kb/notes/designing-agent-memory-systems.md`

Evidence: the target and its backlinks (`kb/notes/agent-memory-requirements/*`, `kb/reference/agent-memory-coverage.md`, `kb/reference/commonplace-agent-memory-gap-plan.md`, tag heads, `kb/agent-memory-systems/README.md` and `types/agent-memory-system-review.md`, related notes and ingests). The current note is a requirements map derived from contextual competence. It says it is "a requirements map rather than a layer architecture" and does not contain the store-everything design, the session-log material, or the ADR/boundary-table material.

1. N — contradicted: "a requirements map rather than a layer architecture", not a design of an ideal system.
2. R (close) — it states its first-principles derivation and its check against the Agent Memory Systems review collection; the "11 systems" count is not visible.
3. N — no store-everything premise; the note says instead that "Raw accumulation does not create usable memory".
4. R — "The pressure is not storage scarcity. The pressure is deciding which retained material should influence a particular loop".
5. N — the store-everything hypothesis is absent.
6. N — the store-everything trade-offs are absent.
7. N — the note puts design weight on selection and shaping, but not on "aggressive, complete, low-ceremony" storage.
8. N — no claim that it inverts other designs' emphasis.
9. R — "Treat Memory As Context Engineering"; the Relevant Notes entry on context efficiency "grounds the context-scarcity pressure".
10. R (close) — cites "Knowledge storage does not imply contextual activation" with its gloss; the "asks the right questions" part is absent.
11. R (close) — "Cheap storage creates candidates ... selecting and shaping those candidates before the context window is spent"; links the preserve-evidence-without-loading-history requirement.
12. R — "Go Beyond Retrieval"; the boundary lists "preference, procedure, decision, or learned constraint".
13. N — the agency trilemma is absent.
14. N — the three-space lifecycle separation and its metabolic-rate argument are absent; only work-surface contamination is mentioned.
15. N — navigability vs retrieval (link networks vs vector search) is absent.
16. N (close) — the open problems name structural pattern detection and discovery extraction, not synthesis as the frontier.
17. N — statelessness and routing as prosthetics are absent.
18. R (close) — "session-trace capture" is named among the gaps in the build-order section, and the gap-plan backlink rests on it.
19. N — keeping compiled views aligned and supersession cover nearby ground, but updating existing notes when new ones arrive is not addressed.
20. R (close) — "promote only when value exceeds cost" requirement; "surface discovery candidates, not automatically graduate them".
21. N — temporal reasoning is absent.
22. N — session logs are not presented as a primary input; only trace extraction appears as one requirement.
23. N — the contents of session logs are not enumerated.
24. N (close) — the note names activation and "Agents repeat known mistakes", but not these three retrieval challenges over past sessions.
25. R — opening sentence: "designed from the effects memory must have, not from its storage layer".
26. N (close) — provenance and reconstructing purpose appear as consumer needs, but not session logs as decision provenance.
27. N — ADRs and session logs are absent.
28. N — the ADR/log bridge is absent.
29. N — the generalized "why" question is absent.
30. R (close) — "Set The Boundary": regression tests and `AGENTS.md` rules stay ordinary execution substrate unless governed as learned constraints.
31. N — the note's boundary principle is different (material that "needs memory operations"), not "what project artifacts don't preserve".
32. N — the boundary table and its assigned homes are absent.
33. N — the note does not claim memory is strongest where project artifacts are weakest.
34. N (close) — codification as a form move is present, but not a graduation into project artifacts with session logs kept as provenance.
35. N — contradicted: the note is explicitly "not a layer architecture".
36. N (close) — the transcript-archive bullet and the trace-extraction requirement touch this, but raw logs are not related to a library layer, and no extraction bridges are named.
37. N — intermediate representations are absent.
38. N (close) — "situation cues" appears only in the gap-plan list; cue-based retrieval for action is not addressed.
39. N — pre-generated activation cues are absent.
40. N — the expertise gap is absent.
41. N (close) — the trace-extraction requirement is linked, but the extraction operations are not enumerated.
42. R — "Corrections and failures have visible signals; discoveries often have only surprise, elaboration, or later reuse".
43. N — the "seen twice on different dates" heuristic is absent.
44. R (close) — "What Remains Open": the boundary between learned memory and work-surface authority is domain-dependent.
45. N — graduation pathways to ADR, procedure, linting rule, or test are absent.
46. N — the relation to manual distillation (ADRs, conventions) is absent.
47. R — last open paragraph: learned memory-management policy vs inspectable heuristics.
48. R (close) — "relax into learned policy where feedback is good, and keep artifact-side control where feedback is weak".
