# Recoverability — target 6

Target: `kb/instructions/analyse-agentic-system/SKILL.md`

Evidence: the target, its backlinks (notably `kb/agentic-systems/COLLECTION.md`, `kb/agent-memory-systems/COLLECTION.md` and `README.md`, `kb/instructions/analyse-agent-memory.md`, `kb/instructions/analyse-external-system-epistemic-architecture.md`, `kb/types/agentic-system-analysis-result.md`), and `kb/types/instruction.md` (named by item 10). The current target makes both lenses mandatory, so items that make a lens optional are contradicted by it.

1. R — opening paragraph: "Analyse one external agentic system at one frozen evidence boundary".
2. N — contradicted: step 5 says "Both lenses always run"; nothing shows that lenses apply only where applicable.
3. R — trigger description ("Use when asked to analyse, review, or refresh ...") and the evidence tiers in steps 2 and 8.
4. R — steps 1–2 fix boundary kind, revision and source register, and step 3 requires every negative or uncertain finding to name its boundary.
5. R — step 4.1 traces one invocation end to end; the footer rests-on note separates scheduling, context assembly, and external state.
6. N — contradicted: the memory lens always runs; the text never asks whether it applies.
7. N — contradicted: the epistemic lens always runs; the text never asks whether it applies.
8. R — step 3 lists the conclusion statuses and forbids upgrading one to the next.
9. R — step 6 requires a system-organized synthesis that preserves conflicts and uncertainty.
10. R — frontmatter `type: types/instruction.md`; file sits in `kb/instructions/`.
11. N — nothing in the document marks it as provisional.
12. N — the document neither names the workshop lens files nor forbids promoting them; it already invokes separate lens instructions.
13. R — step 3/5 treat memory and epistemic analysis as lenses of one run; `agentic-systems/COLLECTION.md` says "Memory and knowledge are lenses of the whole-system analysis"; the footer cites the crosscutting-concern note.
14. R (close) — `agent-memory-systems/COLLECTION.md` and `README.md` call that collection legacy and say that new analysis enters through this skill.
15. N — contradicted: memory analysis runs whether or not the system has memory.
16. R — step 6: "Write a system-organized synthesis ... Do not concatenate lens reports".
17. R — frontmatter `user-invocable: true` with a "Use when ..." description.
18. R — the whole body is a prescriptive analysis-and-publication workflow, and the authorization paragraph scopes what it writes.
19. R — step 3: "The parent owns scheduling, canonical IDs, integration, and recovery. ... Workers do not publish or delegate"; steps 6–10 keep reconciliation, validation, and reporting with the parent.
20. R — steps 1–2 ("Freeze and inspect sources once").
21. R — "Freeze and inspect sources once"; the frozen memory input; step 4.9 "Reuse the memory specialist's findings ... rather than tracing those mechanisms twice".
22. R — "same frozen boundary" for lens workers; Verify: "One run ID, frozen source boundary ... govern every finding".
23. R — step 4, the runtime baseline.
24. N — contradicted: the lenses are not optional, so there is no applicability disposition; only depth is recorded.
25. N — contradicted: the memory lens runs unconditionally, and the text states no read-back trigger for it.
26. N — contradicted: the epistemic lens runs unconditionally, and the text states no truth-apt trigger for it.
27. R (close) — "Both lenses always run", so inspection cannot wait on a prior knowledge-production conclusion.
28. R — step 3 canonical register and step 6 reconciliation by exact IDs.
29. R — step 6: "do not silently strengthen its findings"; step 3 forbids upgrading any status.
30. R — step 6 synthesis; step 3 keeps behavioral authority and uncertainty per route.
31. R — step 6: "Do not concatenate lens reports", "Recheck shared-route ownership", "preserve anchored conflicts".
32. R — step 1.4 scope list.
33. R — step 2.4 keeps evidence layers distinct; step 8 keeps code- and doc-grounded tiers separate.
34. R — step 4.1 and 4.7 enumerate these runtime responsibilities.
35. R (close) — delegated to `analyse-agent-memory`; step 3 defines read-back and activation; the delegation is visible and backed by that instruction's backlink.
36. R — step 5 delegates to the epistemic instruction, which backlinks with its material-route boundary.
37. N — contradicted: no early exit; a thin lens yields a brief result, "not a skipped lens".
38. R — step 6: no "product ranking, generic adoption advice".
39. R (close) — step 6 forbids a "universal maturity model"; step 1.5 allows "another defined class"; the footer cites the note that the design space remains open.
40. R (close) — the authorization paragraph excludes "auxiliary indexes or surveys ... other retained reports"; the memory COLLECTION says old reviews "are not automatically converted or redirected".
41. R (close) — the authorization paragraph limits writes to run, review, and retained result; step 8 leaves comparison outputs to "separate authority"; the type/COLLECTION/schema files are not named.
42. R — step 3: "Static shipped material and ordinary current-run state are not read-back".
43. R — step 3: "Never upgrade context presence to activation"; footer cites storage-does-not-imply-activation.
44. R — step 3: never upgrade "curation to warrant".
45. R — step 2.4 evidence layers, step 4.4 causal support, step 4.7 capability vs grant vs deployed envelope.
46. R — step 3: "Epistemic and operational authority remain separate"; `BAP-*` records.
47. R (close) — step 7: the `complete` disposition does not claim publication; do not persist validation receipts or review-job details in the result.
48. R — the body is a stepwise procedure with explicit decision points; the instruction type requires this.
49. R (close) — the body keeps rationale out and puts it in footer notes; the instruction type says "Keep rationale minimal".
50. R — title "Analyse an Agentic System".
51. R — "Use when asked to ..." description.
52. R (close) — frontmatter names the channel (user-invocable skill, forked context), and the description names the trigger and consumer; no explicit consumer/channel/force statement.
53. R (close) — no `kb/work/` links; the instruction type forbids relying on a link the executor need not open.
54. N — the document already fixes a one-file exact result plus a compact review and gives no sign the choice is open.
55. N — the document invokes separate lens instructions and gives no sign the choice is open.
56. R — backlinks: `agent-memory-systems/README.md` "Current analysis uses analyse-agentic-system"; `analyse-agent-memory` is "commissioned by analyse-agentic-system".
57. R (close) — step 4.6 "explicit inapplicable or uninspected reasons instead of empty fields" and step 4.9 "Unknowns and inapplicable steps remain explicit"; it conflicts with 59 only at the lens level.
58. R — Verify: "thin evidence produces a bounded brief result, not a skipped lens"; step 3 forbids turning omission into absence.
59. R — step 5: "Both lenses always run"; depth is `brief` or `full`.
60. R — step 5: "A brief result still states what was inventoried, what was found, and which conclusions its thin evidence prevents".
61. R — step 5 scoping record plus Verify "Both lenses and both scoping records exist".
