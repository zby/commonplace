# Recoverability — target 1

Target: `kb/notes/context-operation-interface-bounds-context-policy.md`

1. R — title and last sentence of the opening paragraph state it.
2. R — paragraph 3 says policy gains add no view outside `Reach` and do not show excluded projections unnecessary.
3. R — the opening paragraph defines the term almost verbatim.
4. R — the `Reach(I, S, x; M, B)` paragraph and "A policy selects legal traces" state the mechanism.
5. R — the "Evaluation consequence" section states it.
6. R — the evaluation section addresses evaluators; the backlinks are agent-memory reviews and runtime notes (close call: no explicit audience statement).
7. R — the opening paragraph separates the interface from the controller or policy.
8. R — "Architectural contrasts": interfaces "permit different legal traces even though each constructs bounded model context".
9. R — paragraph 3 and the evaluation section state it.
10. R — the evaluation section: "identify which coordinates stayed fixed and which changed".
11. R — claim title, general formalism; Scroll appears only as footer evidence.
12. R — each system is cited for one contrast with its evidence status marked ("paper-described", "not verified executions") (close call).
13. N — nothing in the text says the claim must survive the removal of any one system description.
14. R — frontmatter declares `types/note.md`; the file sits in `kb/notes/`.
15. R — present, and required by the declared type.
16. R — present in title and traits.
17. R — the mechanism occupies the first three paragraphs; the declared type requires it.
18. R — `## Scope` exists and states real limits.
19. R — the declared type's frontmatter table and trait list govern (the item names this type).
20. R — the type spec defines `synthesis` as composing multiple cited claims, distinct from the `has-comparison` trait the note already carries (close call).
21. R — comparisons are visibly subordinated to the one claim ("make the interface variable concrete").
22. N — nothing in the text forbids adding a second durable claim, e.g. when folding in a neighbor (close call with 21).
23. R — claim title and three brief contrasts; a survey would visibly leave the claim (close call).
24. R — Scope: "The claim does not rank interfaces."
25. N — the note has no numbers and says nothing against adding benchmark results; backlinked ingests carry such numbers.
26. R — Scope paragraph 2 states the reliability/safety/inspectability tradeoff and that no interface is ranked.
27. N — the text does not mention novelty assessment.
28. R — paragraph 3 and the evaluation section state it.
29. R — Scope: an open-ended interface "remains bounded by its primitives, sandbox, permitted compositions, model competence, and budget".
30. R — the text marks "practitioner account", "paper-described", and footer "code-grounded" distinctly (close call: marked but not declared as a rule).
31. R — the opening paragraph separates retained substrate from active context.
32. R — Scope: "Structural reach is not achieved use"; links storage vs activation.
33. R — contrasts paragraph: arbitrary programs, typed combinator language, two fixed memory operations.
34. R — contrasts paragraph 2 names model, learned controller, host/proxy, and mixed push/pull.
35. R — "Within-run, cross-restart, and cross-task retained state".
36. R — "Changing a retained invocation policy is not by itself a change to operation semantics".
37. R — Scope: "Exact retained material can coexist with a lossy or missed active projection."
38. N — the term "action alphabet" does not appear; nothing warns against adopting it.
39. R — the term is used and bolded as the defined term.
40. N — "alphabet" and its KB sense appear nowhere in the note or its backlinks.
41. N — the evaluation list matches the axes, but nothing restricts adding other comparison axes (close call).
42. N — the note has no table and says nothing about when one is allowed.
43. R — contrasts are organized by coordinate rather than by system (close call: structural cue only).
44. N — four of the six notes are linked inline; `rlm-has-the-model-write-ephemeral-orchestrators-over-sub-agents.md` and `compiling-coordination-preserves-primitive-not-aggregate-authority.md` are absent.
45. N — nothing restrains adding systems or completing system details plausibly.
46. N — Scope does record limits, but nothing states the rule that unsupported claims appear only as limitations (close call).
