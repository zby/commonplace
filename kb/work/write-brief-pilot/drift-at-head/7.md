# Recoverability — target 7

Target: `kb/instructions/analyse-external-system-epistemic-architecture.md`

Evidence: the target, its one backlink (`kb/instructions/analyse-agentic-system/SKILL.md`, which invokes it as the epistemic lens), and `kb/types/instruction.md` (named by item 7).

1. R — opening paragraph and description: "Determine whether and how ... produces knowledge"; step 9 excludes storage, retrieval, and reshaping.
2. R — "Use this procedure when asked whether or how ..."; the evidence layers cover code- and document-grounded sources.
3. R — outputs 2 and 3 and step 4 (objects and content edges).
4. R — outputs 3–5 (ledger, lifecycle, claim comparison).
5. R — ledger fields separate epistemic authority from operational authority; step 5 defines both.
6. R — output 6 and step 9 state what counts as knowledge production.
7. R — frontmatter `type: types/instruction.md`; file in `kb/instructions/`.
8. R (close) — it covers "memory subsystem or whole agentic system", has a standalone mode beside the orchestrated mode, and ties to no collection; "collection-neutral" is implied, not stated.
9. R — "return evidence-bounded findings rather than a system-wide epistemic grade".
10. R — step 4: "novelty, fluency, and plausibility establish candidate generation only"; step 9 acceptance requirements.
11. R (close) — a description exists and the backlink shows explicit invocation, but the description is phrased as an action, not a "Use when" trigger; the instruction type requires a trigger description.
12. R — "The analysis informs a review; it does not itself accept the external system's claims"; repeated in Verify.
13. R — step 9 and the first misuse guard.
14. R — step 4 classes, plus the direct-adaptation branch.
15. N — the document asks for system-specific object names and never lists these object kinds; an editor would not know the list.
16. R — output 4 lifecycle schema and step 6.
17. R — output 3 ledger fields (target, evaluator, timing, force, both authorities).
18. R — step 5 "operative result" definition; "A recorded result with no consequential consumer has no implemented force"; misuse guard on labels and grades.
19. R — step 7 separations and the fourth misuse guard.
20. R — outputs 3–4 per route; step 9 "Conclude by route".
21. R — output 6 "Do not give the system a single epistemic score, oracle, status"; misuse guard.
22. R — "Keep these evidence layers separate" list.
23. R — "Scope and prerequisites" section and step 1.
24. R — step 3 early branch.
25. R — step 2 "Inventory material objects before evaluators".
26. R — output 3 with its field schema.
27. R — output 4 and Verify "Every truth-apt object has the applicable ... disposition".
28. R — output 5 and step 8.
29. R — output 6 bullet list.
30. N (close) — the misuse-guards section exists, but nothing shows it carries the workshop cases; it largely repeats the steps, so an editor shortening the document could drop or trim it without warning.
31. R — the stepwise body with its outputs is executable; the instruction type requires this.
32. R (close) — the body carries only definitions needed for decisions; the instruction type says "Keep rationale minimal".
33. R — "read natural-language content, test symbolic artifacts ..., and use available probes for distributed-parametric state".
34. R — step 5 lists "human, model, program, environment, proof, measurement, or hybrid" evaluators.
35. R — early branch (no claims), lifecycle states (`not reached`), step 7 (consequence fit vs mechanism warrant), step 9 (accepted, scoped claims).
36. R — gap/limit columns; Verify "Every negative or unknown is scoped".
37. R — scope branch: product ranking is out of scope.
38. R — step 7 "bundle success from the effect of an individual component"; attribution rule.
39. R — scope branch and last misuse guard.
40. R — scope branch and last misuse guard ("universal ontology").
41. N — the document already defines controlled status tokens and says nothing against adding matrix schema or further tokens.
42. R — title "Analyse an External System's Epistemic Architecture".
43. R — Scope and prerequisites, Steps, Verify.
44. R (close) — the frontmatter holds only these two fields; the instruction type says extra fields come only from a runtime consumer.
45. R — no `kb/work/` links; the instruction must be context-complete per its type.
46. R — output 6 and Verify: "gives no system-wide epistemic grade".
47. R (close) — the document names no specific system; its worked example is generic; no explicit rule.
48. R — standalone mode, explicit schemas, and per-system route findings make it system-independent.
