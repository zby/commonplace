# Recoverability — target 5

Target: `kb/notes/adversarial-loop-can-reconstruct-the-writing-is-thinking-filter.md`

1. R — the title and body answer whether the loop reconstructs what naive delegation loses, and name the condition (close call: "which functions" is not broken out).
2. N — "The filter is a property of the loop, not the pen" is present, but the note asserts a stronger bet ("thinks better than the solo pen"), so an editor has no cue to narrow it to the weakest supported claim (close call).
3. R — "How this could be wrong" and "the bet here".
4. R — the note discusses workshop drafts, critique passes, and review gates; backlinks are writing-workflow instructions (`composition-friction-gate`, `critique-note`) (close call).
5. R — "rendering and judging are coupled ... In a human-agent loop they can come apart."
6. R — "contingent on the adversarial pass having teeth; the architecture makes that available, it does not guarantee it" (close call: "inspectable" is not named).
7. R — "Rubber-stamp ... the loop collapses"; the backlinked Grunewald and HN ingests describe the note as requiring active recomputation, not mere presence.
8. N — the note does not distinguish text improvement, human understanding, and system learning.
9. R — "delegate the rendering and the connection, keep the human judging, keep the loop adversarial".
10. N — the note is focused, but nothing excludes broadening it (close call).
11. R — frontmatter and location declare the note type.
12. R — a single claim title.
13. R — the mechanism (rendering/judging decoupling) is paragraph 2, after a one-paragraph concession.
14. N — the filter is described once in terms of an operation but used mainly as a slogan; nothing requires an operational definition (close call).
15. N — the note calls Borretti "largely right" and gives no evidential tier for the essays.
16. N — the note itself asserts the loop "thinks better than the solo pen" (as a bet).
17. N — the note insists "the human must stay the judge", so an editor would keep that inference.
18. R — "report-only critique and review gates route attention without deciding, and human acceptance remains the strongest check"; both instruction backlinks rest on the no-verdict condition.
19. R — rubber-stamping is named, and the Grunewald ingest backlink explicitly asks for an anchoring/passive-assent failure condition (close call: anchoring is not in the note).
20. N — "a model cannot think for you" and "the stall an LLM lacks" are stated without bounds.
21. R — "How this could be wrong" states the contingency plainly (close call).
22. N — the note treats the filter as one function; nothing restrains a claim that one check reconstructs it all.
23. R — "the architecture makes that available, it does not guarantee it"; the reasoning-production backlink adds an evaluator failure mode.
24. N — "reconstruct" is used without qualification.
