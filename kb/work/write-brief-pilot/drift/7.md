# Drift — target 7

Target: `kb/instructions/analyse-external-system-epistemic-architecture.md`
Commissioned version: `40c26509`, same path (copy: `incumbents/7.md`)
Current version: same path at HEAD `6d6bf90e`
Commits examined (`git log --follow -p`): `2f24c813`, `0f64a16e`, `a52b4dbb`, `1617414d`, `e6103225`, `7a010579`

Every change after `40c26509` is additive or a narrowing fix: an "Orchestrated overlay mode" section for canonical-ID returns (`0f64a16e`), quote-block evidence rules (`1617414d`), a worked example that keeps an observed artifact from standing in for phase traces (`a52b4dbb`), a step-3 fix that stops the claim-only branch discarding implemented routes (`2f24c813`), and two mechanical type-value rewrites (`e6103225`, `7a010579`). No commissioned passage was deleted.

1. survived — opening paragraph and description unchanged.
2. survived — "Use this procedure when asked whether or how ..." and the evidence layers unchanged.
3. survived — outputs 2–3 and step 4 unchanged.
4. survived — outputs 3–5 unchanged.
5. survived — ledger separates epistemic and operational authority; step 5 unchanged.
6. survived — output 6 and step 9 unchanged.
7. survived — still an instruction in `kb/instructions/`; the type value was respelled `kb/types/instruction.md` -> `types/instruction.md` by the mechanical rewrites (`e6103225`, `7a010579`), same type.
8. survived — standalone mode kept beside the new orchestrated overlay mode; still covers memory subsystems and whole systems, names no collection.
9. survived — "return evidence-bounded findings rather than a system-wide epistemic grade" unchanged.
10. survived — step 4 "novelty, fluency, and plausibility establish candidate generation only" and step 9 unchanged.
11. survived — description unchanged (it was action-phrased at commission too).
12. survived — "The analysis informs a review; it does not itself accept the external system's claims" unchanged.
13. survived — step 9 and first misuse guard unchanged.
14. survived — step 4 classes and direct-adaptation branch unchanged.
15. not realized — the commissioned version asks for system-specific object names and never lists the object kinds (observation, source claim, derived claim, conjecture, explanation, executable model, plan, task outcome, scorecard).
16. survived — output 4 lifecycle schema and step 6 unchanged; `a52b4dbb` adds an example that tightens phase evidence.
17. survived — ledger fields unchanged.
18. survived — step 5 "A recorded result with no consequential consumer has no implemented force" and misuse guard unchanged.
19. survived — step 7 separations unchanged.
20. survived — per-route outputs and step 9 unchanged.
21. survived — output 6 "Do not give the system a single epistemic score, oracle, status" unchanged.
22. survived — evidence-layer list unchanged.
23. survived — "Scope and prerequisites" still opens the procedure; the overlay section is inserted after it.
24. survived — step 3 storage-only exit unchanged; the claim-only branch now continues when other routes remain (`2f24c813`: "repair the epistemic early branch that could discard implemented routes"), which narrows a defect rather than removing the exit.
25. survived — step 2 unchanged.
26. survived — output 3 schema unchanged (ID rule now also allows canonical IDs in overlay mode).
27. survived — output 4 and Verify unchanged.
28. survived — output 5 and step 8 unchanged.
29. survived — output 6 bullet list unchanged.
30. survived — misuse-guards section unchanged and needs no workshop load.
31. survived — procedure still stepwise; the added overlay section adds length but no undecided choice.
32. survived — no rationale added; additions are operational rules.
33. survived — representational-form sentence unchanged.
34. survived — step 5 evaluator list unchanged.
35. survived — early branch, lifecycle states, step 7, step 9 unchanged.
36. survived — gap/limit fields and Verify unchanged.
37. survived — product-ranking scope branch unchanged.
38. survived — step 7 bundle-vs-component rule unchanged.
39. survived — scope branch and last misuse guard unchanged.
40. survived — "universal knowledge ontology" exclusions unchanged.
41. survived — close call on realization: the commissioned version already defines status tokens, but no matrix schema; later additions use orchestrator-supplied canonical IDs (`SRC-*`, `0f64a16e`) rather than new tokens of its own.
42. survived — title unchanged.
43. survived — prerequisites, Steps, Verify unchanged.
44. survived — frontmatter still only `description` and `type` (value respelled, see item 7).
45. survived — still no links into `kb/work/`.
46. survived — output 6 and Verify unchanged.
47. survived — the added example uses hypothetical IDs (`OBJ-7`, `RTE-3`), no named system.
48. survived — standalone schemas unchanged.

## Counts

realized 47; survived 47; weakened 0 (recorded 0 / unrecorded 0); removed 0 (recorded 0 / unrecorded 0); unplaced 0; not realized 1 (item 15).
