# Drift — target 6

- Target: `kb/instructions/analyse-agentic-system/SKILL.md`
- Commissioned version: `b23f1e07`, same path (copy in `incumbents/6.md`)
- Current version: same path at HEAD `6d6bf90e` (working tree clean for this file)
- Commits examined: all 41 commits touching the path in `b23f1e07..HEAD` (bodies read); diffs read for `2f24c813`, `0bcc3b89`, `5fdf75d9`, `6b550e26`; phrase removals located with `git log -S`. Also read: ADR 083, `kb/work/analyse-agentic-system/README.md` at `2f24c813`, `kb/types/agentic-system-analysis-result.md`, `kb/instructions/analyse-agent-memory.md` (the specialist instruction the skill now dispatches).
- Conflict note: the workshop README records the user decision of 2026-08-21 that "both lenses run on every in-scope system", which "supersedes the original boundary item, 'explicit applicability dispositions'". The commissioned version already implements item 59, so the items that conflict with 59 are not realized in it.

1. survived — opening sentence still "Analyse one external agentic system at one frozen evidence boundary".
2. not realized — commissioned version runs both lenses always (step 5); applicability gate resolved in favour of 59 before commission.
3. survived — trigger description ("Use when asked to analyse, review, or refresh ...") kept; code/doc evidence layers kept.
4. survived — boundary, revision and uncertainty in steps 1–3; the overall tier moved from the skill body to the result type the skill writes (`evidence-tier` required field), so still binding.
5. survived — step 4 traces one invocation end to end (context, state, effects, coordination).
6. not realized — no "whether the lens applies" decision in the commissioned version (conflict with 59); the analysis half is realized.
7. not realized — same as 6 for the epistemic lens.
8. survived — conclusion-status list kept (now eight values, `wired` added between `afforded` and `observed`).
9. survived — step 6 system-organized synthesis preserving conflicts and uncertainty.
10. survived — `type: types/instruction.md` in `kb/instructions/` (type-value spelling changed mechanically by `e6103225`/`7a010579`).
11. not realized — commissioned text never marks itself provisional; only conditional clauses ("Until a dedicated result contract exists") hint at it.
12. survived — no workshop lens file was promoted; the memory specialist (`kb/instructions/analyse-agent-memory.md`) descends from the retired legacy review writer (`6b550e26`) and is a parent-commissioned subprocedure since `281532ed`.
13. survived — memory and epistemic remain lenses of one run; footer "why memory is a mandatory lens".
14. survived — footer still rests on "Agent memory is a crosscutting concern, not a separable niche"; description now names memory systems as in scope of this skill.
15. not realized — memory lens runs unconditionally in the commissioned version (conflict with 59).
16. survived — step 6: "Do not concatenate lens reports"; synthesis organized around the system.
17. survived — `user-invocable: true` with a "Use when ..." description.
18. survived — body remains a prescriptive analysis-and-publication workflow (the explicit force sentence is gone; see 52).
19. survived — "The parent owns scheduling, canonical IDs, integration, and recovery ... Workers do not publish or delegate"; memory lens now must (not may) run in a fresh specialist.
20. survived — steps 1–2 "Freeze and inspect sources once".
21. survived — one frozen source register handed to the specialist; 4.9 "Reuse the memory specialist's findings ... rather than tracing those mechanisms twice".
22. survived — lens workers get "the same frozen boundary"; memory input freezes full revisions and is hashed.
23. survived — step 4 runtime baseline.
24. not realized — commissioned version records depth, not an applicability disposition (conflict with 59).
25. not realized — trigger evidence only sets depth in the commissioned version (conflict with 59).
26. not realized — same as 25 for the epistemic lens.
27. survived — "Both lenses always run", so inspection cannot wait on a success conclusion; the explicit sentence "Successful knowledge production is never a prerequisite" was dropped in `5fdf75d9` but the property holds structurally.
28. survived — canonical register owned by the orchestrator; reconciliation maps proposals to canonical IDs.
29. survived — "do not silently strengthen its findings"; never-upgrade rules.
30. survived — step 6 bounded synthesis with separate evidence status per property.
31. survived — "Do not concatenate lens reports", "Recheck shared-route ownership", preserve anchored conflicts.
32. survived — scope still covers runtimes, orchestration, operating layers and narrower model-dependent mechanisms; "harness" dropped as a named kind in `2f24c813`, replaced by the rule "Preserve harness only when it is the source's own term; use agent runtime ...".
33. survived — evidence layers kept distinct (step 2.4); code/doc tier carried by the result type; step 8 "numerical claims use code-grounded rows, with doc-grounded findings kept separate".
34. survived — step 4.1/4.7/4.9 cover scheduling, context, state/effects, coordination, permissions, approval, recovery, observability and retained instructions (the governance-surfaces footer link was dropped in `5fdf75d9`).
35. survived — moved: memory coverage now lives in `analyse-agent-memory.md` (write side, read-back, availability/delivery/activation, trace learning), which the skill binds a fresh specialist to execute.
36. survived — step 5 invokes the epistemic instruction with classify-only routes.
37. not realized — commissioned version has no early exit for a lens ("a brief pass is a result, not an omission"); conflict with 59.
38. survived — "Do not ... add a product ranking, generic adoption advice".
39. survived — "universal maturity model" forbidden; "or another defined class" in step 1.5.
40. survived — neither version directs corpus relocation or retrofit; current says legacy snapshots "are not rebuilt".
41. survived — current text directs no changes to collection contracts, schemas or matrices; the explicit clause "Do not change schemas or parsers" was dropped in `0bcc3b89` ("Define typed agentic analysis results"), when the result type the condition waited on was built.
42. survived — step 3: "Static shipped material and ordinary current-run state are not read-back".
43. survived — "Never upgrade context presence to activation"; the anti-conflation line "retaining material is not selecting it into context" was cut in `5fdf75d9`, but the specialist instruction still requires tracing "selection and delivery" and separating "availability, delivery, activation".
44. survived — "Never upgrade ... curation to warrant".
45. survived — evidence layers (2.4), capability/grant/deployed envelope (4.7), `causally supported` only for actual interventions (4.4).
46. survived — "Epistemic and operational authority remain separate".
47. survived — step 7: `complete` does not claim publication; "Do not persist a JSON validation receipt or review-job details in the result".
48. survived — stepwise procedure with explicit decision points.
49. survived — rationale remains mostly in footer links.
50. survived — "Analyse an Agentic System".
51. survived — "Use when asked to ..." description.
52. weakened [unrecorded] — 5fdf75d9 removed the sentence "The consumer is an analysing agent or maintainer; the channel is explicit invocation or trigger-matched skill loading; the force is a prescriptive analysis and result-writing policy" inside a 516-line rewrite; the body names ADR 083 machinery ("packet, receipt, reconciliation, diagnostic-ledger, and commit-protocol"), not this statement. Frontmatter still implies channel; force is no longer stated.
53. survived — no `kb/work/` links in the current text.
54. removed [recorded] — 5fdf75d9 ADR 083: "Every run writes one exact result under `kb/reports/state/agentic-system-analysis/<run-id>/result.md`" (commissioned text: "this instruction deliberately does not fix the physical layout").
55. removed [recorded] — 6b550e26 "Make one typed specialist report the memory handoff into the main analysis" (then `281532ed` "Make agent memory analysis a parent-commissioned instruction"): the reserved decision was settled toward separate lens instructions. Commissioned version mixed the two (embedded memory lens, invoked epistemic procedure) and the workshop README listed the question as "Not fixed yet".
56. survived — single public entry point at HEAD. Transient violation: `6b550e26` projected `analyse-agent-memory` as its own user-invocable skill; `281532ed` reversed it, recording "a direct invocation exposed an internal specialist as an independent entry point".
57. survived — "explicit inapplicable or uninspected reasons instead of empty fields"; "Unknowns and inapplicable steps remain explicit".
58. survived — "Do not turn omission of an open-ended mechanism into evidence of absence"; result type retains every required section; the explicit "An absent lens section or file must never carry meaning implicitly" was cut in `5fdf75d9`.
59. survived — "Both lenses always run" with `brief` or `full` depth.
60. survived — "A brief result still states what was inventoried, what was found, and which conclusions its thin evidence prevents".
61. survived — both scoping records remain mandatory (Verify: "Both lenses and both scoping records exist"); depth is enumerated `brief`/`full`, so `uncertain` cannot be a scoping value, though the explicit rule "`uncertain` is not a scoping value and not an exit" was cut in `5fdf75d9`.

## Counts

realized 52; survived 49; weakened 1 (recorded 0 / unrecorded 1); removed 2 (recorded 2 / unrecorded 0); unplaced 0; not realized 9.
