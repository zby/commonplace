# Recoverability — target 1

Target: `kb/notes/context-operation-interface-bounds-context-policy.md`
Commissioned version: `d7615fea` (`incumbents/1.md`). Backlinks at that commit: none.

1. R — title and last sentence of the opening paragraph state the bound under fixed substrate, model, and budget.
2. R — third paragraph: better-policy gains "do not add a view outside `Reach`, nor ... show that excluded projections are unnecessary".
3. R — the bolded definition in the opening paragraph matches nearly verbatim.
4. R — `Reach` defined over legal interface traces in the second paragraph; the third says the policy selects legal traces within it.
5. R — `## Evaluation consequence` states that gains show usefulness in the tested regime, not that excluded operations are unnecessary.
6. N (close) — no audience is stated; the evaluation section implies evaluators, but nothing warns a newcomer rewrite away from runtime and memory-system designers.
7. R — opening paragraph separates the interface from "a controller or context policy [that] chooses whether, when, and how to use" it.
8. R (close) — `## Architectural contrasts` compares systems that "permit different legal traces even though each constructs bounded model context"; the "similar evidence" framing is implicit.
9. R — third paragraph and evaluation section say gains inside a fixed operation set do not establish adequacy.
10. R — evaluation section: "It should identify which coordinates stayed fixed and which changed."
11. R (close) — Scroll is never mentioned; the collection contract's transferable-claim test and the note's general framing keep it from becoming a system description.
12. R — collection contract's formulation constraint; each system is introduced by source kind and tied to the coordinate it illustrates.
13. R — the collection contract's theory-independence constraint states it.
14. R — frontmatter and the note type spec.
15. R — the type spec requires a discriminating description; the incumbent has one.
16. R — claim title and `title-as-claim` trait present; the collection contract requires the trait with a claim title.
17. R — the type spec's writing shape; the mechanism occupies the first three paragraphs.
18. R — `## Scope` present with real limits; the type spec requires it.
19. R — the type spec's frontmatter and trait tables list what is authorized.
20. R (close) — the type spec defines `synthesis` as composing multiple cited claims into one argument, which this single-mechanism note does not do; nothing explicitly warns against adding it.
21. R — the collection contract's body-composability rule; contrasts are framed as making "the interface variable concrete".
22. R — body-composability rule; every section serves the one claim.
23. R (close) — only three systems, each tied to a coordinate; nothing explicitly forbids widening into a survey.
24. R — "not verified executions or a performance ranking"; Scope: "The claim does not rank interfaces."
25. N (close) — nothing warns against adding benchmark numbers; Scope says only that the contrasts do not estimate performance effects.
26. R — Scope, second paragraph: restricted interfaces may trade transformations for reliability, safety, and so on.
27. N (close) — nothing in the text or contracts warns against adding a novelty assessment.
28. R — third paragraph and evaluation section.
29. R — Scope: "An open-ended programming interface remains bounded by its primitives, sandbox, permitted compositions, model competence, and budget."
30. R (close) — sources are tagged "practitioner account" and "paper-described", and the comparison is marked "not verified executions"; no code-grounded review is present to contrast.
31. R — the opening sentence separates the two layers.
32. R — Scope: "Structural reach is not achieved use ... a delivered view may still fail to affect behavior."
33. R — `## Architectural contrasts`, first paragraph: RLM programs, lambda-RLM combinators, ACM fixed operations.
34. R — contrasts, second paragraph lists all four placements.
35. R — contrasts, second paragraph names within-run, cross-restart, and cross-task retained state.
36. R — "Changing a retained invocation policy is not by itself a change to operation semantics or composition rules."
37. R — Scope: "Exact retained material can coexist with a lossy or missed active projection."
38. N — the term "action alphabet" is never used, so nothing warns against introducing it as a synonym.
39. R — the term is bolded, defined, and used consistently throughout.
40. N — conditional item; "alphabet" does not appear, and nothing warns about the collision if an editor introduces it.
41. R (close) — the evaluation section lists the coordinates that matter, which matches the allowed axes; there is no explicit bar on adding other axes.
42. N (close) — the incumbent has no table, and nothing warns against adding a large one.
43. R (close) — contrasts are organized by coordinate, not by system; no explicit instruction preserves that.
44. N — four of six named notes are cited inline; `rlm-has-the-model-write-ephemeral-orchestrators-over-sub-agents.md` and `compiling-coordination-preserves-primitive-not-aggregate-authority.md` are not cited, so nothing tells an editor to cite them.
45. R (close) — each system appears only for the coordinate it witnesses; the collection contract requires stating the proposition a witness supports.
46. R (close) — the collection contract says to name real gaps in Scope or Open Questions; the Scope's last sentence does this.
