# Recoverability: target 6

Target: `kb/instructions/analyse-agentic-system/SKILL.md`. Commissioned version: `b23f1e07` (`incumbents/6.md`). Backlinks at that commit: none listed. Also read: `kb/types/instruction.md`, `kb/instructions/COLLECTION.md`.

Note: the incumbent realizes item 59 (both lenses always run, depth by evidence), so items that conflict with 59 are labelled against that realization.

1. R — opening sentence: "Analyse one external agentic system at one frozen evidence boundary"; user-invocable skill.
2. N — (absent) the incumbent says the opposite: "Both lenses always run"; `inapplicable` is "never a reason to skip a lens".
3. R — description trigger ("asked to analyse, review, or refresh") and consumer line; code/doc-grounded tier in step 3.
4. R — steps 1–2 (boundary, revision), step 3 tier, step 10.4 report, limitations paired with prevented conclusions.
5. R — step 4.2 loop record: next-step owner, context selection, state reads/writes, action executor.
6. R (close) — step 6 covers retention, write side, read-back, activation, `BAP-*` authority; "whether it applies" survives only as step-5 trigger evidence and depth.
7. R (close) — step 7.4 return list (transformation, checking, acceptance, integration, three authorities); step-5 epistemic trigger evidence; applicability recast as depth.
8. R — step 3 conclusion statuses (with `afforded` deliberately replacing `implemented`, explained in text).
9. R — step 8.4: synthesis organized around the system, preserving evidence-layer limits.
10. R — frontmatter `type`, location, type spec.
11. N — (absent) nothing marks the entry point as provisional; only "until a dedicated result contract exists" hints at it.
12. N (close) — (absent) workshop lens files are never mentioned; only "embedded" lens wording and the frontloading rests-on link hint against splitting lenses out.
13. R — rests-on "Agent memory is a crosscutting concern… why memory is a lens inside system analysis, not a peer category"; step 6 "embedded".
14. N (close) — nothing addresses the memory collection's role; only the crosscutting rests-on link and "do not reuse the agent-memory review schema" point that way.
15. R — one skill owns the whole run and runs both analyses every time, which satisfies the conditional.
16. R — step 8.4 "organized around the deployed system's progression … not as concatenated lens reports" (historical-order clause itself not stated).
17. R — `user-invocable: true`, "Use when…" description, channel sentence.
18. R — "the force is a prescriptive analysis and result-writing policy".
19. R — opening ownership paragraph and "Worker topology".
20. R — steps 1–2 "Open one run", "Freeze sources once", one register.
21. R — step 2.4 "Lens workers must not reacquire, refresh, or widen sources".
22. R — step 7.3 "no revision change"; step 8.3 one revision per route.
23. R — step 4 runtime baseline, always run.
24. R (close) — step 5 scoping record per lens with trigger evidence, depth, rationale; prevented conclusions in outputs; but lenses are not optional.
25. R (close) — this exact condition is the step-5 memory trigger evidence; it now sets depth, not whether to run.
26. R (close) — step-5 epistemic evidence names truth-apt routes and knowledge/warrant claims; again depth, not gate.
27. R — "Successful knowledge production is never a prerequisite for running the lens."
28. R — canonical records table, step 8.1.
29. R — "Never upgrade…"; step 7.3 "no silent evidence upgrade".
30. R — step 8.2–8.4 ownership and limits preserved in the synthesis.
31. R — step 8: merge duplicates by ID, preserve conflicts, ownership rules, "not as concatenated lens reports".
32. R — description and step 1.2 scope list.
33. R — step 3 overall tier, per-source layers split by scope.
34. R — step 4.2 fields and 4.4 conditional surfaces; step 6.1 retained operative parts.
35. R (close) — step 6 storage, write side, read-back, activation, accumulated-from-use; "action-capacity support" not named.
36. R — step 5 direct-adaptation exception and step 7 invocation of the epistemic instruction.
37. N — (absent) incumbent forbids exits: "`uncertain` is not … an exit"; no lens early exit exists.
38. R — second paragraph prohibits rankings and adoption advice.
39. R — second paragraph and step 4.4 prohibit a universal taxonomy.
40. N — (absent) corpus relocation or retrofit is never mentioned.
41. R (close) — step 10.3 "Do not change schemas or parsers"; step 9 "Do not improvise a collection contract"; the "before tested" condition and matrices are not stated.
42. R — definition of memory read-back vs retained state.
43. R — step 3 "Never upgrade: context presence to activation", step 4.3, step 6.4.
44. R — truth-apt definition ("curation labels … do not establish … warrant"), step 10.2.
45. R — evidence layers, statuses, step 10.2 distinctions.
46. R — behavioral-authority definition: "Keep all three separate".
47. R (close) — step 6.6 omits checkout/publication mechanics; source register and verification report are separate logical records from the analysis; no explicit sentence.
48. R — collection contract executability tests; frontloaded steps with explicit branches.
49. R — type spec and collection contract "Keep rationale minimal".
50. R — "# Analyse an Agentic System"; collection title convention.
51. R — "Use when…" description; type spec.
52. R — opening names consumer, channel, force.
53. R — collection contract forbids links into `kb/work/`; none present.
54. R — step 9 "this instruction deliberately does not fix the physical layout".
55. N (close) — the incumbent fixes one arrangement (memory lens embedded, epistemic invoked by path) without saying the choice is open; the rests-on link only names the trade-off.
56. R (close) — "This skill owns the whole run"; singularity implied, not stated.
57. R — `inapplicable` and `uninspected` statuses; uncertainty kept as explicit limitation.
58. R — step 5 "An absent lens section or file must never carry meaning implicitly".
59. R — step 5 "Both lenses always run"; depth follows evidence.
60. R — step 5 "floor for a brief output"; Verify section.
61. R — step 5 scoping records plus "exit that means 'we could not tell' reads … as 'there is nothing there'".
