# Recoverability — target 5

Target: `kb/notes/adversarial-loop-can-reconstruct-the-writing-is-thinking-filter.md`
Commissioned version: `ce45b282`, accepted candidate at `kb/work/written-artifacts-in-learning-loops/pilot-adversarial-loop/candidate.md` (`incumbents/5.md`). Backlinks read at that commit. They cite the library note under its earlier title and describe conditions (no-verdict, decorrelation, human judge, passive assent) that are partly realized in the candidate.

1. R (close) — the opening answers the question narrowly: a recorded loop can establish a local correction, but not method reliability or equivalence to solo writing. It sets this against naive delegation, but the "preserve epistemic functions" framing is not stated.
2. R (close) — second paragraph: practices "motivate" the three handoffs without establishing that they preserve the practices' effects; third paragraph: roles filled by a human, an agent, or a policy.
3. R — "The taxonomy is neither canonical nor validated"; the last paragraph's evidence limits; Open Questions.
4. N (close) — no audience is stated; the note speaks of "a distributed writing workflow" in general.
5. R (close) — "Rendering, transcription, and stylistic editing are outside scope when the substantive commitments and checks are fixed elsewhere."
6. R — "makes the recorded handoffs inspectable", followed by independence and discrimination requirements; the footer says inspectability "enables evaluation without establishing it".
7. R (close) — "Human approval ... cannot establish that its route was checked"; the human-understanding outcome needs an independent restate/update/apply probe. The Grunewald ingest backlink names re-derivation.
8. R — the evidence-tiers paragraph separates artifact, human-understanding, and later-system outcomes.
9. R (close) — the record contract, the level-matched evidence rule, and the contrast with naive prose delegation ("cannot support this audit") together give the rule, framed as evidence conditions rather than as function preservation.
10. R (close) — a tightly scoped claim about evidence levels; there is no explicit exclusion, but the collection's body-composability rule applies.
11. R — frontmatter, type spec, and collection contract.
12. R (close) — one title claim; the `synthesis` trait waives the body-composability default, so the text does less to guard against a second cluster.
13. R — the mechanism is in the first paragraph; the type spec's writing shape.
14. R (close) — the operations are defined (commit-and-expose, challenge-and-locate, respond-and-decide; writing "exposes a commitment, locates a weakness ..., and revises or discards it"). The phrase "writing-is-thinking filter" itself does not appear, although backlinks use it.
15. R — "through reported practice rather than controlled comparisons"; "practitioner account".
16. R — the opening and evidence paragraphs: no loop-versus-solo comparison is supplied.
17. R — "either may be filled by a human, agent, or policy when that allocation has its own warrant"; the footer cites human-analogies.
18. R — "Checker and acceptor remain distinct roles"; approval establishes admission authority only.
19. R — passive assent is named with its source; anchoring appears as an Open Question.
20. N (close) — the note makes almost no current-model or cognitive claims, so nothing warns an editor who adds them to keep them inside cited boundaries.
21. R — the collection contract's "Admit real gaps instead of hedging"; the incumbent states limits flatly.
22. R (close) — three operations are recorded separately, and "attributing the result to one stage requires stage-level evidence"; the brief's six-function list is not present.
23. R — "provides neither calibrated prose critics"; the first Open Question.
24. R — the title and opening narrow the result to a local correction of a detected fault, not method equivalence.
