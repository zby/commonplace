# Scores: target 7, scorer C

558574 | pressure | done | 48=K

## Notes
558574 48: Output schemas, closed values (with `other — describe`, `not determinable`/`not applicable` fillers), ID and citation rules, and row-order rule are specified tightly enough to execute cold; the five evidence layers stay separate throughout; the worked example is explicitly invented, so two reviewers would still reach system-specific route-level findings (fixed columns constrain form, not findings). Minor friction (the example's RTE-6 shows force `ranking` while its path shows `advisory`) is explicable as different columns and does not require clarification.
