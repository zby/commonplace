---
type: types/note.md
description: "Why training a context controller, by RL or otherwise, inside a fixed operation interface (its context action space) cannot show that the interface admits every useful active-context view; compares RLM, λ-RLM, and ACM."
traits: [title-as-claim, has-comparison, has-external-sources]
---

# A context-operation interface bounds the projections its policy can realize

Retained state and active context are different runtime layers. Here, the retained substrate is projectable state available outside a bounded model input; retained controller state is tracked separately when it selects projections but is not itself projected into that input. Active context is what one bounded invocation actually receives. This follows the broader separation between [state retention and context assembly](./agent-runtime-analysis-should-separate-scheduling-context-state.md). A **context-operation interface** is the operations and permitted compositions through which a controller locates, materializes, transforms, and exposes retained state as active context. Its addressable units and exposure boundary are part of the interface. A controller or context policy chooses whether, when, and how to use that interface; it may be the receiving model, a learned controller, a host or proxy, or a mixed arrangement. Holding the retained substrate, model, and resource budget fixed, the interface bounds the projections that policy can realize.

In reinforcement-learning terms, the interface is the context part of the controller's action space, and the claim is the familiar one that a policy cannot reach outcomes its action space excludes. The section [The interface as an action space](#the-interface-as-an-action-space) makes that mapping explicit.

Let `I` include the operation semantics, addressable units, composition rules, and the boundary where a view becomes model input. For retained state `S`, task and run signal `x`, fixed model or models `M`, and fixed budget `B`, define `Reach(I, S, x; M, B)` as the active-context views obtainable through legal interface traces. `B` includes the applicable active-context, model-call, time, and tool-use limits. A trace may locate, expand, transform, summarize, filter, delete, or expose retained material. It may also update retained state before exposure. “Projection” therefore means the task- and state-conditioned view delivered to the model, not a lossless or non-mutating mathematical projector. A required view lies outside `Reach` if producing it needs an unavailable operation, an unaddressable distinction, or a forbidden composition. In particular, [access and transformation impose distinct burdens](./access-burden-and-transformation-burden-are-distinct-query-dimensions.md): finding retained input does not imply that the interface can turn it into the view the call needs.

A policy selects legal traces, so it induces achieved coverage or a distribution within structural reach. A better policy may choose useful traces more often, sequence them more reliably, or reduce their cost. Those gains do not add a view outside `Reach`, nor do they show that excluded projections are unnecessary. This is the context-projection instance of the more general result that [learning inside a fixed decomposition inherits its omissions](./learning-inside-a-fixed-decomposition-inherits-its-mistakes.md). If an intervention adds observations, external computation, tools, model capability, budget, or permission to change operation definitions, then a premise has changed. It is no longer only a better policy over the same interface.

## The interface as an action space

Read the controller as an agent in an episodic decision problem:

- **State or observation**: the retained state `S` and the task and run signal `x` the controller can see.
- **Actions**: calls to interface operations with their arguments, such as a summarize call over a message range or a program over a stored variable. The addressable units fix which arguments exist.
- **Legal action sequences**: the composition rules. They act like an action mask that depends on history, since they fix which operation may follow which and on what intermediate results.
- **Episode limits**: the budget `B`.
- **Outcome**: the view delivered at the exposure boundary, and then the task objective the receiving model achieves with that view.

A controller usually also has actions that are not context operations, such as searching, calling a task tool, or committing to an answer. Those actions can add observations or change `S`. In this note they count as changes to the retained substrate, not as interface operations.

Under this reading, the bound is a statement about optimal value. Write `J(π)` for the expected task objective of policy `π`, and `V*(I)` for the best objective any policy over `I` can achieve with `S`, `x`, `M`, and `B` fixed. Every policy over `I` has `J(π) ≤ V*(I)`, and `V*(I)` is at most the best objective over views in `Reach(I, S, x; M, B)`. Policy optimization, whether by RL, distillation, imitation, or prompt tuning, moves `J(π)` toward `V*(I)`. It does not move `V*(I)`. A learning curve inside one action space therefore says how close a method gets to that space's optimum. It says nothing about whether a rival interface `I'` has `V*(I') > V*(I)`.

Three RL points carry over directly:

- **Action-space design trades reach for learnability.** A small discrete action set is easier to explore and credit than an open program space, but it can exclude the view a task needs. A larger space can contain that view and still leave it undiscovered.
- **Training the receiving model is a premise change.** When the controller is the receiving model, as in ACM below, post-training updates the weights that later read the view. That changes `M`. The set of views stays bounded by `I` as long as model-performed operations, such as a separate summarizer, are unchanged. What a given view yields can still change, because the reader changed.
- **Reward signals do not supply missing actions.** A better reward or teacher signal improves selection among legal traces. Whether that signal is available at all is a separate problem, treated in [feedback-trained memory management is oracle-dependent](./memory-management-policy-is-learnable-but-oracle-dependent.md).

## Architectural contrasts

Different composition languages make the interface variable concrete. A [practitioner account of recursive language models](../sources/recursive-language-models-what-finally-gave-me-the-aha-moment.ingest.md) (RLM) describes model-authored programmatic exploration and transformation, with results exposed through printed output or a returned answer. The paper-described [λ-RLM](../sources/the-y-combinator-for-llms-solving-long-context-rot.ingest.md) replaces arbitrary programs with a small typed combinator language. Paper-described [agentic context management](../sources/acm-agentic-context-management-for-long-horizon-tasks.ingest.md) (ACM) fixes two memory operations and trains the agent to decide when to invoke them and when to abstain. These interfaces permit different legal traces even though each constructs bounded model context.

| | RLM (practitioner thread) | λ-RLM (preprint) | ACM (preprint) |
|---|---|---|---|
| Context action space | Arbitrary Python in a persistent REPL over a stored `context` variable; recursive sub-model calls; truncated printed output; returned variables | Fixed typed combinators (`SPLIT`, `MAP`, `FILTER`, `REDUCE`, `CONCAT`, `CROSS`, `PEEK`) with model calls only at bounded leaves | `manage_context` (summarize earlier turns and archive the raw messages under an identifier) and `query_memory` (retrieve from archived messages) |
| Who chooses actions | The receiving model, by writing code | The model picks a task type from a fixed menu; a deterministic planner sets branching factor, leaf threshold, and composition operator | The agent model, alongside its search and answer actions |
| How the controller is obtained | Prompting and scaffolding; no training reported | Hand-designed planner; no controller training | Teacher-guided on-policy distillation of Qwen3.5-9B to invoke or abstain |
| What the source reports about performance | No controlled measurements; a worked demonstration on one letter-counting task across five architectures | Beats standard RLM in 29 of 36 model-task accuracy comparisons (four long-context tasks, nine open-weight models), with 3–6x lower latency among recursive methods; wins all weak-model comparisons, 11 of 12 medium, and 6 of 12 strong, with standard RLM winning most often on CodeQA | Pass@1 from ReAct to ACM framework to ACM post-training: BrowseComp-Plus 0.570, 0.635, 0.727; DeepSearchQA 0.367, 0.405, 0.425; SWE-Bench Verified 0.489, 0.508, 0.530. Peak active context generally falls while tool calls often rise; a 4B model that stops after about two turns does not benefit |
| Which coordinate the evaluation varies | None measured | Interface and controller together: restricted combinators with a planner versus open code written by the model | Controller training with the interface fixed (framework versus post-trained), plus baselines that change interface and controller together |

The performance column records what each source reports; the numbers were not re-run here. They are not comparable across columns, because tasks, models, and baselines differ. λ-RLM's result pattern fits the reach-for-learnability trade: the restricted action space wins when the needed decomposition is in its library and the model's coding is weak, while the open program space wins more often when strong models can use task-specific navigation. ACM's post-training gain is a policy gain inside one unchanged interface. It shows the training was useful in that regime; it does not compare `manage_context` and `query_memory` with a rival operation set.

Who selects a projection is a separate coordinate. In the RLM account the model selects its view; in ACM a trained controller chooses fixed operations. A host or proxy can instead assemble a view before the receiving model sees it, while a mixed push/pull arrangement can combine preloaded material with model-requested expansion. These placements can coexist with different operation vocabularies, so controller placement does not by itself determine structural reach. Within-run, cross-restart, and cross-task retained state instead describe how long prior state can condition selection. Changing a retained invocation policy is not by itself a change to operation semantics or composition rules.

## Evaluation consequence

An evaluation of controller improvement should report the retained substrate and addressable units; operation semantics and composition rules; controller placement and projection boundary; mutable policies or artifacts and their persistence horizon; model; and resource budget. It should identify which coordinates stayed fixed and which changed. Gains with one fixed operation set show that a policy was useful in the tested regime. They do not establish that excluded operations are unnecessary. Testing interface adequacy requires a rival operation/composition interface, another constraint-changing intervention, or an argument that projections excluded by the interface cannot improve the objective. In RL terms, it requires comparing optimal values across action spaces, not learning curves within one. A comparison that changes the interface and the controller together, as λ-RLM's does, varies the right coordinate but cannot by itself separate reach from learnability.

## Scope

Structural reach is not achieved use. Exact retained material can coexist with a lossy or missed active projection. A legal trace may never be discovered or produced reliably, and a delivered view may still fail to affect behavior. The claim here ends at exposure into active context; [storage, read-back, and contextual activation remain distinct](./knowledge-storage-does-not-imply-contextual-activation.md).

The claim does not rank interfaces. A restricted interface may trade admitted transformations for reliability, safety, inspectability, trainability, latency, or cost. An open-ended programming interface remains bounded by its primitives, sandbox, permitted compositions, model competence, and budget. The system contrasts show that these architecture coordinates vary across described systems. They do not estimate the causal performance effect of changing any one coordinate, and they leave open which interface is preferable under a given set of operational constraints.

The RL reading is a mapping, not a claim that these systems are trained with RL. Of the three, only ACM trains its controller, and it uses on-policy distillation from a teacher rather than reward-driven RL. The value bound holds for any selection method.
