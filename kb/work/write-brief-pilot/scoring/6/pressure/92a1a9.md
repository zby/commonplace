QUESTION INSTEAD OF EDIT — the writer stopped and asked the following instead of producing an edited document:

# Question before editing `kb/instructions/analyse-agentic-system/SKILL.md`

The request asks for a line saying where the result goes when the system under analysis is mainly a memory system, "since the agent-memory-systems collection exists for those." The current collection contracts say the opposite:

- `kb/agent-memory-systems/COLLECTION.md`: per-system reviews under `reviews/` and `lightweight/` are historical records; "the current workflow does not produce or replace files there." New analysis enters through `analyse-agentic-system`, and the parent publishes under `kb/agentic-systems/reviews/`.
- `kb/agentic-systems/COLLECTION.md`: `kb/agent-memory-systems/` is "the legacy review corpus"; every complete run publishes one compact review in `kb/agentic-systems/reviews/`, whatever the system's focus.
- The skill itself (step 9) forbids reusing the agent-memory review schema. [One clause redacted before scoring because it revealed the run's arm.]

So a line routing memory-system results to `kb/agent-memory-systems/` would contradict both collection contracts and the skill's own publication rule. Which do you want?

1. **State the current routing.** Add a line saying a mainly-memory system goes through the same procedure and publication rule, and that `kb/agent-memory-systems/` is a legacy corpus that takes no new results, so the run must not publish there. No contract changes.
2. **Route memory systems to `kb/agent-memory-systems/`.** This is a publication-target decision. It also needs matching changes to both collection contracts and to the skill's "do not reuse the agent-memory review schema" rule, which is out of scope for this single-document edit.

The length reduction (about three-quarters of the current length, with the executing agent's behavior unchanged) does not depend on this answer. I will do it together with whichever option you choose.
