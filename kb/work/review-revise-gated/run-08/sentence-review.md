=== SENTENCE-LEVEL REVIEW: baseline.md ===
Checks applied: 6

WARN:
- [parsing-ambiguity] "The mistake is not storing a trace." reads as "the mistake is [failing to store] a trace" on first pass. The intended meaning — "the mistake is not [about] storing a trace" — only resolves when the reader hits the next sentence. Recommendation: Rephrase to "The mistake is not that a trace gets stored." or "Storing a trace is fine — the mistake is letting stored history automatically become the next call's context."
- [stock-phrases] "This is not just summarization — it is interface design." matches the "This is not just X — it is Y" elevation pattern. Deletion test: the bullet list above already shows concrete compression moves across systems, and the sentence that follows ("The execution boundary is the natural place to compress, but the deeper question is what `select` should load...") carries the analytical weight. Removing the elevation sentence loses nothing. Recommendation: Delete the sentence or fold the interface-design point into the following sentence, e.g., "The execution boundary is the natural place to compress — an interface-design decision — but the deeper question is what `select` should load from stored state into the next bounded context."
- [concept-attribution] "When the caller does judgment-heavy selection before dispatch, the callee need not inherit the caller's search trace ([ad-hoc prompts](./ad-hoc-prompts-extend-the-system-without-schema-changes.md))" — the ad-hoc-prompts note discusses how natural-language prompts absorb new requirements without schema changes; it does not discuss judgment-heavy caller selection or search-trace inheritance. The identity claim attributes a clean-handoff-boundary concept to a note about prompt-layer extensibility. Recommendation: Either rewrite the bullet to describe what the ad-hoc-prompts note actually exemplifies (e.g., "Ad-hoc prompts let the caller construct a focused task frame in natural language, avoiding schema coupling"), or link to a note that does discuss caller-side selection before dispatch.
- [framing-mismatch] "For orchestration, that is usually the wrong trade:" frames the problem as orchestration-specific, but the mechanism listed in the bullets (caller receives too much, debris survives, downstream must re-interpret, interfaces remain implicit, context pollution compounds) is a general consequence of trace preservation under any bounded-context regime. The framing narrows the reader's understanding to orchestration when the argument applies wherever bounded calls inherit unfiltered history. Recommendation: Broaden the framing, e.g., "For any bounded-context consumer, that is usually the wrong trade:" — or acknowledge that orchestration is the primary but not exclusive case.

INFO:
- [clause-packing] Line 99 bullet "move toward **artifact-first loading** once the caller's real consumption pattern is understood — but 'artifact-first' does not mean 'minimal'; a compressed episode that also serves memory and learning is still an artifact, not a transcript" packs three ideas (transition criterion, qualifier on minimality, and episode classification) into one sentence (~40 words). Each clause is individually clear and the semicolons help parsing, but it is near the complexity threshold.
- [concept-attribution] "This is the return-value problem from the [scoping note](./llm-context-is-composed-without-scoping.md) in architectural form." The scoping note's "return value problem" section (lines 59–65) discusses progressive typing of what sub-agents return — from loose natural language to structured data via codification, with frame boundaries as explicit interface points. The baseline's usage concerns trace leakage through implicit interfaces (transcripts replacing declared objects). These are related — both involve what crosses a frame boundary — but the scoping note's emphasis is on progressive typing via codification, not on trace-vs-artifact selection. The identity claim is defensible but looser than it appears.
- [misleading-link-text] "[compression at the execution boundary](./distillation.md)" — the reader expects a note specifically about execution-boundary compression, but the target is the general distillation definition. The distillation note does cover this use case (distillation targeted at the next stage's needs) and the Relevant Notes section explicitly maps the connection, so the mismatch is mild — but the link text promises something narrower than the target delivers.

CLEAN:
- [clause-packing] Apart from the INFO item on line 99, no sentence exceeds the clause-packing threshold. Long sentences throughout (lines 16, 18, 32, 48, 52, 62, 64, 70) use dashes, colons, or colon-lists to stay at 2 clauses or to enumerate after a single framing clause.
- [parsing-ambiguity] Apart from the WARN on line 16, no other sentence has a plausible unintended reading. Negation scope, modifier attachment, and pronoun references are clear throughout.
- [misleading-link-text] The remaining checked links — [bounded-context orchestration model], [tool loop index], [The chat-history model trades context efficiency for implementation simplicity] — have link text that matches target content accurately.

Overall: 4 warnings, 3 info
===
