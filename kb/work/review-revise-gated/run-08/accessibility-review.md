=== ACCESSIBILITY REVIEW: baseline.md ===
Checks applied: 4
WARN:
- [jargon-persistence] "bounded call" / "bounded context" / "bounded execution" appears ~10 times in the body after being introduced at line 14. By the practical-principle section (lines 95-102), "a specific bounded call truly needs it" and "the next bounded call should inherit" rely on the reader still holding the KB-specific meaning from paragraphs ago. Recommendation: After the opening establishes the term with its link, switch to plain language ("the next call", "each call") in the body, reserving the full "bounded call" phrasing for the opening and Relevant Notes section.
- [jargon-persistence] "external symbolic state" is introduced at line 13, reused at lines 18, 52, and 64 without refresher context. By the "storage vs next-context loading" section (line 52), "The scheduler can afford to keep many artifact kinds in external symbolic state" expects the reader to recall the KB-specific meaning. Recommendation: Replace later occurrences with plain language such as "the scheduler's stored state" or "persistent storage," keeping the full term only at first use.
- [notation-opacity] `K`, `select(K)`, and `P` are notation from another note (the bounded-context orchestration model) used in prose without local definition. Line 16: "storage in `K` is cheap; bounded context is expensive" — the sentence is opaque without knowing what `K` stands for. Line 22: "stores the result in `K`" — same issue. Line 48: "the prompt assembled by `select(K)`" — requires prior knowledge of the notation. Line 62: "loading happens through `select(K)`." Recommendation: Replace notation with plain language throughout. E.g., line 16 could read "storing artifacts in the scheduler's state is cheap; loading them into a prompt is expensive." Only keep `K`/`select(K)` notation if the note makes formal arguments (it does not).
- [undefined-terms] "execution boundary" is used at line 11 ("An execution boundary usually creates two different questions") without inline definition or gloss. The reader must infer what qualifies as an execution boundary. Recommendation: Add a brief gloss on first use, e.g., "An execution boundary — any point where one LLM call ends and another begins — usually creates two different questions."
- [undefined-terms] "clean model" at line 62 ("In the clean model, loading happens through `select(K)`") is used without definition. The reader cannot tell what "clean" contrasts with. Recommendation: Replace with a self-explanatory phrase such as "In the deliberate-loading model" or "In the model where loading is selective," or gloss it inline.
- [unidentified-references] "Slate" is introduced at line 68 as "Slate is the main tension case" without identifying what Slate is (who built it, what kind of system it is). The linked source title ("Slate: Moving Beyond ReAct and RLM") provides some context but does not appear inline. Recommendation: Add identification on first mention, e.g., "Slate, an agent orchestration system described in [public description](...), is the main tension case." (The note does not state who built Slate, so the author should supply that attribution if known.)
- [unidentified-references] "Spacebot" at line 78 is introduced only as a bullet-list item ("[Spacebot](./related-systems/spacebot.md) branches return only a scrubbed conclusion") without identifying what Spacebot is. Recommendation: Add a brief identification, e.g., "Spacebot, a multi-agent research system, branches return only a scrubbed conclusion." (The note does not say who built Spacebot; the author should supply that if known.)
INFO:
- [jargon-persistence] "execution boundary" appears 3 times (lines 11, 74, 81) — borderline, but the term is close enough to plain English that later uses remain interpretable without scrolling back.
- [notation-opacity] `select` without parenthesized `K` at line 81 ("Compression at the boundary produces the artifact; `select` decides whether and how much to load") is slightly less opaque but still assumes the reader knows this is a function name from the orchestration model.
- [undefined-terms] "tool loop" (lines 18, 28) is used without inline definition, but it is linked on first substantive use (line 28) and is close enough to standard terminology that most readers can infer the meaning. Borderline.
- [undefined-terms] "phatic turns" at line 56 is a linguistics term that may be unfamiliar to the target audience (engineers working with LLMs). Not KB jargon, but potentially opaque.
CLEAN:
- [jargon-persistence] "trace-preserving," "transcript inheritance," and "artifact-first loading" are introduced with enough surrounding context that they remain readable on reuse.
- [notation-opacity] No formal equations or pseudocode appear in the note, confirming that the notation serves no formal purpose and plain-language replacement is appropriate (supporting the WARN findings).
- [undefined-terms] Standard technical terms (LLM, context window, prompt, API, SDK) are used appropriately without unnecessary definition.
- [unidentified-references] All other proper nouns and named concepts are either standard vocabulary or linked with sufficient inline context.
Overall: 7 warnings, 4 info
===
