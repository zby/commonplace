=== COMPLEXITY REVIEW: baseline.md ===
Core claim (one sentence): Higher-level interfaces (chat sessions, framework-owned tool loops) silently promote stored execution traces into the next call's context, conflating the storage decision with the loading decision that should be governed by selective projection from external symbolic state.
Checks applied: 4
WARN:
- [connection-inflation] Five Relevant Notes entries restate relationships the body already explains fully, adding no new navigational value. Inflated entries: (1) conversation-vs-prompt-refinement — footer says "conversation preserves trace, prompt refinement compresses it," but lines 87-88 say the same thing verbatim. (2) ad-hoc-prompts — footer says "the caller does judgment-heavy selection before dispatch," which is nearly identical to line 77. (3) Spacebot — footer says "branches return scrubbed conclusions," which restates line 78. (4) Slate ingest — footer says "episodes are compressed return artifacts, not tactical transcripts," which restates lines 68-69 and 79. (5) distillation — footer says "execution-boundary compression is distillation targeted at the next stage's needs," which the body establishes at lines 74 and 81. Recommendation: Trim these five footer entries to short navigational pointers (e.g., role labels only) or remove the explanatory gloss that duplicates body text. Keep the entries that add genuinely new framing (bounded-context orchestration model, codification-and-relaxing, agent-orchestration design space).
INFO:
- [claim-to-section-ratio] 7 distinct non-obvious claims across 8 sections — ratio is acceptable. The weakest section is "Conversation vs refinement is one instance of the general problem" (lines 83-91), which functions primarily as a pointer to a separate note rather than advancing a new claim. It re-frames the storage-vs-loading distinction through a lens already delegated to the linked note. Not a WARN because it does add the context-cloning/forking variant (line 89) and the section is short, but it is the most expendable section if the note needs trimming.
CLEAN:
- [could-be-a-paragraph] The note carries at least 7 distinct non-obvious claims: the storage/loading separation, the packaging-layer origin of the conflation, the exploratory-default rationale for trace preservation, the breakdown mechanism (preserves everything), the three-trace-type taxonomy with differential loading profiles, the cross-system compression pattern with external evidence, and the graduated practical principle. A single paragraph would lose the trace taxonomy, the cross-system evidence, and the store-early/artifact-later graduation. The multi-section structure is justified.
- [framework-decoration] The three-item trace taxonomy (conversation transcripts, tool/action traces, reasoning traces) is load-bearing: it establishes that the argument against loading varies in strength by trace type, with reasoning traces being the clearest case and tool traces the most nuanced. The bullet structures in "Where the problem actually appears" and the practical principle likewise carry distinct content that a prose rewrite would not compress without loss.
Overall: 1 warning, 1 info
===
