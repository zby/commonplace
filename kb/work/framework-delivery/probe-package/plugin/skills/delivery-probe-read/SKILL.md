---
name: delivery-probe-read
description: Probe skill for the delivery test. Use only when asked to run the delivery-probe-read check.
type: kb/types/instruction.md
---

# delivery-probe-read

1. Run `cp-delivery-probe-instruction shared-step`, read the file it prints, and follow it.
2. Run `cp-delivery-probe-library`, then read `types/probe-note.md` under the root it prints.
3. Report both tokens verbatim, and the absolute path of each file you read.

If you cannot run commands, use the relative links instead, resolved against this skill's real directory (this skill may be a symlink): [shared step](../../library/instructions/shared-step.md), [probe note type](../../library/types/probe-note.md).
