# Traversable provenance chains

Layer: ingestion / provenance.

Derived claim → intermediate synthesis → source-span, walkable both ways, so "if this source is retracted, what claims fall?" is a graph query, not a manual audit.

The command-level expression of this idea is [`commonplace-source-impact` and derived dashboards](./dashboards-and-source-impact.md).
