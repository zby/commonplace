**Pass.** The note has four mechanism sections (system prompt files, capability descriptions, memory, configuration injection) plus a design-principles section. The distinct non-obvious claims are: (1) four structurally different always-loaded surfaces exist with different read/write cadences; (2) capability descriptions are always listed but body-on-demand, which is structurally different from system prompt files; (3) memory's central design tension is the write-policy question, not content; (4) configuration injection is partial evaluation of instructions; (5) volatile project state is a gap that fits no surface well. Five substantive claims, roughly five sections — the ratio holds. The design-principles section synthesizes rather than restates by identifying which surface distinctions are load-bearing (ambient vs on-demand, write cadence mismatches).
