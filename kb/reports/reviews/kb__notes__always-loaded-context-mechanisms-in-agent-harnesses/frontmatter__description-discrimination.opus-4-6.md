pass — The description adds substantial retrieval value beyond the title. It names the four specific mechanisms catalogued (system prompt files, capability descriptions, memory, configuration injection), flags that write policies are compared, and signals the gaps angle — none of which is implied by the title alone. An agent could distinguish this from a general "context loading" note using the description.
