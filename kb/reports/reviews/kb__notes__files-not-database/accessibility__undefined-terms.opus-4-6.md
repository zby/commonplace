The term "qmd" is used three times without definition or gloss:

- "the way qmd already works for semantic search"
- "qmd already works this way for semantic search"
- "semantic search via qmd"

The reader can infer it is a semantic search tool, but the name itself is opaque. On first use, add a brief identification, e.g., "qmd (the project's semantic-search index builder)" or similar phrasing that tells the reader what it is.