**pass** The title makes a contestable claim. An informed reader could argue that end-to-end tests with real tools are sufficient for testing LLM instructions, since mocking introduces its own failure modes (mocks that don't reflect real tool behavior) and the overhead of maintaining a mock layer may exceed the benefit. The note addresses this by framing mocking as dependency injection at the natural seam (tool calls), enabling isolation testing that end-to-end tests cannot provide at the instruction level.
