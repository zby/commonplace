**Pass.** The seven Relevant Notes entries are diverse and each adds navigational value beyond what the body contains. `memory-management-policy-is-learnable` is flagged as exemplifying the predicted hybrid — the body mentions it briefly as "the clean case" but doesn't develop the AgeMem details. `oracle-strength-spectrum` adds the gradient refinement that the binary boundary framing in the body doesn't cover. `codification-and-relaxing` operationalizes the boundary in a way the body explicitly defers ("For how to operate... see"). The source entries add empirical grounding not present in the body prose. None of the entries merely restate what the body already explains.
