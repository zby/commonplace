"Oracle strength," "hard-to-soft oracle range," "delayed-oracle," and "no-oracle zones" appear in the generator/verifier section without any local definition. The sentence "The viability of generator/verifier varies along the oracle spectrum: it works well in the hard-to-soft oracle range but breaks down in the delayed-oracle and no-oracle zones where the quality gate can't discriminate" is opaque without knowledge of the oracle-strength-spectrum note's framework. A reader encountering this for the first time would need to follow the link to decode the sentence. Add a brief grounding on first use — e.g., "oracle strength (how cheaply and reliably you can check whether output is correct)" — so the spectrum terminology makes sense without the link.
