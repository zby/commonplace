pass. The title makes a contestable architectural claim: the right response to a semantic sub-goal that exceeds one context window is to treat it as a scheduling problem (symbolic loop + bounded calls), not to extend the context window or use a monolithic large-context call. Someone could reasonably argue the opposite — that increasing context capacity is the correct solution, or that re-prompting with summaries is sufficient. The title earns its place as a design principle, not a tautology.
