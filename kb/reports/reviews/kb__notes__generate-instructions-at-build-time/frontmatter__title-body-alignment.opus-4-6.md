**pass** The body supports the heading title "Generate KB skills at build time, don't parameterise them" — it covers the two options (runtime variables vs build-time generation), argues for generation, and extends into installation-specific inputs that follow the same pattern. Minor scope note: the "installation-specific inputs" section extends beyond KB skills to recurring tasks and local config, but this broadens rather than contradicts the title's claim.
