"typed-callable" appears in the body with a link but no inline definition:

> "the [typed-callable](./instructions-are-typed-callables.md) pattern applied to configuration dependencies"

The term is KB-specific (it means an instruction document that declares its input/output types like a function). A reader encountering this note independently gets only the link — the surrounding phrase "Injection scope. Not every task needs every config value. The build step should resolve only what the task declares it needs" gives some contextual clue, but does not explain what "typed-callable" means. A brief inline gloss would make the sentence self-sufficient.
