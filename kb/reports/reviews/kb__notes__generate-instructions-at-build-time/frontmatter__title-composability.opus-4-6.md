**fail** The filename-derived title "generate instructions at build time" is an imperative phrase that reads awkwardly in prose frames: "since generate instructions at build time..." is ungrammatical. The heading title "Generate KB skills at build time, don't parameterise them" has the same problem — it is an imperative command, not a declarative claim, so it cannot be embedded in "since [title]..." without rewriting. The note should either adopt a declarative title (e.g., "build-time generation of KB skills eliminates per-use interpretation overhead") or treat the current heading as a valid topical/procedural label if it is used as such.
