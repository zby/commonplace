**pass** The description adds mechanism (template generation pays the flexibility cost once at setup) and contrast with the alternative (runtime variables pay it on every use, with occasional LLM misreads). This is materially more informative than the title alone — it names the cost structure that makes build-time generation preferable and mentions the failure mode (misreads) that the title does not hint at.
