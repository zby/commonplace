PASS

The note draws an analogy between compiled languages (resolve at compile time) and LLM instructions (resolve at build time). The bridge is explicit: "It's constraining applied to configuration — the template is soft, the generated output is hard." The programming-to-LLM transfer is grounded in a shared mechanism (eliminating runtime interpretation cost), not asserted by proximity.