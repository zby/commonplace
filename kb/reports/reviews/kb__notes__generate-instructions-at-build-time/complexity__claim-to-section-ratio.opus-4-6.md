**Pass.** The note has two sections beyond the opening argument: the core claim (build-time generation beats runtime variables) and "Installation-specific inputs" (a second category of build-time inputs that vary per installation). Each section carries a distinct claim. The installation-specific section adds genuinely new content about local config, recurring tasks, injection scope, and graceful absence — not a restatement of the opening argument.
