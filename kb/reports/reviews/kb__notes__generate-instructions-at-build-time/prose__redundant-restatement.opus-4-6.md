PASS

The opening paragraph of "Installation-specific inputs" introduces a genuinely new concept (values static per installation but varying between installations) rather than restating the main claim. The bold labels ("Injection scope", "Graceful absence") each introduce distinct design patterns without restating prior material.