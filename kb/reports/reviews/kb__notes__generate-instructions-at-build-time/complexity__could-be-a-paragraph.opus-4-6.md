**Pass.** A paragraph version would capture "generate skills from templates at build time rather than using runtime variables," but would lose the installation-specific inputs category (values static within one installation but varying between installations), the recurring-task application, the injection-scope and graceful-absence design points. These are independently useful claims that justify the structure.
