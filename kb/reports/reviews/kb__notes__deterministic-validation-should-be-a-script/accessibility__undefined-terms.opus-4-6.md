- "hard oracle" and "soft oracle" — used as section headers and organizational labels without inline definition. The note links to the oracle strength spectrum but does not define these terms inline. The listed examples provide implicit meaning (hard oracle = mechanically checkable, soft oracle = requires judgment), but the terms themselves are unexplained. A reader encountering "hard oracle" as a section label without context would need to follow the link.

Recommendation: Add a brief gloss on first use of "hard oracle" and "soft oracle," e.g., "Hard oracle (mechanically checkable — no judgment required)" and "Soft oracle (judgment-dependent — requires LLM or human review)." This can be done inline with the section headers or as a brief line before the list.

- "Areas-Topics footer sync" — used in the hard oracle list as a known operation ("already have `sync_topic_links.py`"). This is internal KB vocabulary. Readers outside the project won't know what "Areas-Topics footer sync" refers to. Since the note is KB-internal documentation, this may be acceptable, but the gloss "already have `sync_topic_links.py`" provides only partial context (it names the script, not what the operation does).

Borderline — acceptable for internal documentation but could be clearer.
