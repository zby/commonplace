- "hard oracle" / "soft oracle" — introduced via the linked [oracle strength spectrum](./oracle-strength-spectrum.md) as section labels without inline definition in this note. Both terms appear twice (as section headers and in the closing paragraph). The note uses them as organizing labels, but readers who haven't read the oracle strength spectrum note won't know what "hard oracle" and "soft oracle" mean as a classification axis.

However, the note partially glosses them through the listed examples: "hard oracle" items are things like "YAML parse, no duplicate keys" (mechanically checkable), and "soft oracle" items are things like "description quality (discrimination, mechanism vs summary)." These examples convey the meaning even if the terms themselves are not defined. The gate threshold is whether a term appears more than twice after its definition point and loses grounding — here the terms appear only twice each with adjacent examples. Borderline pass.

pass
