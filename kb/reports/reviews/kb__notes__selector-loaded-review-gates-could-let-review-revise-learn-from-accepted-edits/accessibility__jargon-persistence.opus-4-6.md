"Oracle" is introduced at line 67 with an implicit gloss ("This keeps the oracle where it is strongest: not 'was the critique clever?' but 'did a human actually accept this change?'") but then reused at lines 108, 109, 110, and 133 without re-grounding. By "The proposal does not remove the oracle problem. It only narrows it" (line 108) and "A human-accepted edit is a strong oracle for..." (line 109), the reader must recall the initial implicit gloss. Suggest adding an explicit inline definition on first use, e.g. "This keeps the oracle (the correctness signal the system trusts) where it is strongest."