**Pass.** All eight footer entries provide navigational value not fully stated in the body. The `spec-mining` entry ("accepted edits can be mined into reusable review gates") adds the spec-mining framing explicitly — the body describes the mechanism but doesn't use the "spec mining" framing itself. The `methodology-enforcement-is-constraining` entry names the "enforcement gradient" that the maturation path section refers to by paraphrase only. The `quality-signals-for-kb-evaluation` entry adds that promotion and retirement need composite signals — this is not stated in the body, which only describes promotion criteria in vague terms. The `automating-kb-learning` entry adds a scope condition (this proposal narrows the automation target) that the body implies but doesn't state explicitly. The `a-functioning-kb-needs-a-workshop-layer` entry adds the library/workshop framing that the body uses informally without invoking the formal distinction.
