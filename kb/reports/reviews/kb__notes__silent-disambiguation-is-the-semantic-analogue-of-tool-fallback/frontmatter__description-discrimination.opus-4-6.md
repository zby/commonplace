pass. The description adds mechanism and scope that the title elides: it specifies the exact failure mode (unacknowledged material ambiguity resolved silently), the consequence (final success hides that the contract failed to determine the path), and frames this as an extension of an existing problem (tool-fallback observability). An agent searching on underspecification or observability would immediately distinguish this note from general discussion of ambiguous specs.
