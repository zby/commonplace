- "two-tree installation layout" is introduced in the body: "This note covers the repo's own layout, distinct from the [two-tree installation layout](./adr/006-two-tree-installation-layout.md) that emerges when commonplace is installed into another project." No inline definition is given for "two-tree." The term appears again in "The [installation layout ADR](./adr/006-two-tree-installation-layout.md) calls for `kb/types/` to hold global types." Both uses rely on the reader knowing what "two-tree" means. Since the note explicitly says it is covering "the repo's own layout, distinct from the two-tree layout," a reader can infer "two-tree" refers to the installed project's layout structure, but the term itself is unexplained. See undefined-terms gate.

- "maturity ladder" is used twice: "global types — the maturity ladder (`text` and `note`)" and "Putting the maturity ladder in CLAUDE.md." A brief inline description is given: "the maturity ladder (`text` and `note`)." Both uses are close together. No flag.

- "collection types" and "global types" are introduced with a distinction explained inline: "Collection types (notes/types/, sources/types/, tasks/types/) define concrete structural templates... Global types define when a document promotes from one maturity level to another." Used consistently. No flag.
