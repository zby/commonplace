- "two-tree installation layout" is used without inline definition: "distinct from the [two-tree installation layout](./adr/006-two-tree-installation-layout.md) that emerges when commonplace is installed into another project." The term "two-tree" is not explained — a reader doesn't know what makes it "two-tree" (presumably two directory trees, but this is not stated). Recommend adding a brief gloss: "two-tree installation layout (the design where the repo's source tree and the installed project's content tree are kept separate)."

- "ADR" is used in "The [installation layout ADR](./adr/006-two-tree-installation-layout.md)" without expansion. "ADR" is a standard term (Architecture Decision Record) but the note uses it as if the reader already knows it. Given that the KB has an `adr/` directory, this is likely deliberate, but a reader new to the KB may not know it. Standard enough to not flag, but borderline.
