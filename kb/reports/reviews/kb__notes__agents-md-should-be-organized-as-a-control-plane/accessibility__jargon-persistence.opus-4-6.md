- "control plane" is introduced in the title and opening sentence: "AGENTS.md is not just a style file. It is the runtime control plane for agent behavior in a repository." The term is used seven or more times throughout the note. "Control plane" is a term borrowed from networking/systems (the layer managing routing decisions vs. data forwarding). The note does not define it inline — it uses the term as if the meaning is obvious. A reader from a non-infrastructure background may not know what "control plane" means. The note's own content (the three-layer model of invariants, routing, escalation) shows what a control plane does, but the term itself is introduced without explanation. Recommend adding a brief gloss on first use, e.g., "the runtime control plane — the always-loaded layer that governs routing, constraints, and escalation decisions."

- "operations catalogue" appears three times: in the placement rule table, in the Exclusion rules, and in the Lifecycle section. The term is introduced without inline definition. A reader cannot tell from context whether this is a specific artifact type in the KB or a generic concept. Recommend adding a brief gloss on first use, e.g., "operations catalogue (a staged collection of externally triggered procedures not yet promoted to skills or instructions)."
