INFO

The note's title and opening claim generality about AGENTS.md organization. The body uses this repository's specific structure as illustrative material in two places:

1. **Escalation boundary examples** (lines 67–70): reference `kb/sources/`, `.ingest.md`, `WRITING.md`, and local `types/` templates. These are explicitly labeled "Concrete escalation examples," so the framing is correct — the reader understands they are illustrative.
2. **Lifecycle step 4** (line 104): references `kb/instructions/` and promotion to skills — specific to this KB's architecture.

INFO rather than WARN because the examples are explicitly framed as examples, not presented as the only possible instantiation. A reader from a different project would understand the pattern without confusion. However, if the note is ever extracted for reuse outside this KB, the examples would need generalization or explicit scoping ("in this repository, for instance…").
