- "token pressure failure" and "attention dilution failure" are introduced as named failure modes: "Two distinct failure modes sit behind this goal: **Token pressure failure**: critical instructions are omitted or truncated because always-loaded context is too large. **Attention dilution failure**: critical instructions are present but drowned by low-value text, so execution quality drops anyway." Both are defined inline with the bolded label followed by a colon-expansion. Adequate as written. Pass.

- "CI playbook" appears in the placement rule: "externally triggered -> operations catalogue or scheduler/CI playbook." "CI playbook" is not defined or glossed. A reader may not know what a CI playbook is in this context (CI = continuous integration? a specific artifact type?). Recommend either glossing or replacing with plain language, e.g., "scheduler or automated-pipeline procedure."

- "heartbeat" appears in the Exclusion rules: "periodic hygiene operations (heartbeat/user/scheduler triggered)." "Heartbeat" is used as a trigger type without explanation. In the context of agent systems, "heartbeat" could mean a periodic scheduled signal. A brief gloss would help: e.g., "periodic hygiene operations (triggered by a timer, user action, or scheduler)."
