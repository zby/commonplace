PASS

No bridge-paragraph duplication found. Transitions between sections are clean:

- The paragraph before "Core model" introduces failure modes (token pressure, attention dilution) — the Core model section then introduces the two placement variables and the placement rule, which is new content.
- The paragraph ending the Core model ("The same placement also controls both cost modes…") is a one-sentence summary of the model's dual coverage, not a preview of the layers section that follows.
- Each subsequent section (Nested topology, Exclusion rules, Lifecycle, Quality tests, Open questions) begins with its own material without being previewed by the preceding section's closing text.
