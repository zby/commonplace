pass — The title functions naturally as a linkable prose fragment. Frames like "since LLM context is composed without scoping, prompt injection is structurally unavoidable" or "because LLM context is composed without scoping, sub-agents are the only isolation mechanism" read fluently. The verbal phrase "is composed without scoping" gives the title action and makes it easy to embed in causal or explanatory sentences.
