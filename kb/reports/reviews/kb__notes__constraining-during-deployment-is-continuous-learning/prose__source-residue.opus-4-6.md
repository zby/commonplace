**PASS**

The note's claimed generality is about deployed AI systems, and all examples sit within that domain: `format_date()` functions, system prompts, validation scripts, DSPy/ProTeGi, developer workflows with AI agents, and agent memory systems. These are not leakage from a narrower source domain — they are the primary domain the note addresses. The examples are diverse enough to support the general claim without overcommitting to any single sub-domain.
