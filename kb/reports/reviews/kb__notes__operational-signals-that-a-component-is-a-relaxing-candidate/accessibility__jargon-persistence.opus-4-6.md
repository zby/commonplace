Two KB-specific shorthand terms are introduced early and then reused throughout the note without ongoing context:

1. "Vision features" / "vision-feature" is introduced via "others are vision features (spec is a theory)" and then reused as compressed shorthand at least 5 more times (lines mentioning "vision-feature pattern," "vision-feature components," "encodes a vision feature rather than a genuine spec," "locking in a vision feature as if it were arithmetic"). By the later uses, the reader must recall that "vision feature" means "a component whose specification encodes a theory about the problem rather than the problem itself." Suggested fix: after the first few uses, occasionally expand the shorthand, e.g. "encodes a theory (vision feature)" or "is theory-laden rather than definitional."

2. "Relaxing" is used throughout (title, "relaxing signal," "relaxing candidate," "relaxing is cheap," "relaxing signals detect") but is never explicitly defined in this note. The reader must infer from context and the bitter-lesson-boundary link that relaxing means moving a component from codified to learned. Suggested fix: add an inline definition on first use in the body, e.g. "relaxing -- shifting a component from a codified implementation back toward a learned one --" or similar.