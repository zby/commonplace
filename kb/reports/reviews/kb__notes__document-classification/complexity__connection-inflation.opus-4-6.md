**Pass.** The four Relevant Notes entries each add navigational value. `note base type` and `text root type` are the actual type definition files the spec delegates to; the body explicitly points readers there. `document-types-should-be-verifiable` holds the design rationale explicitly excluded from this spec. `directory-scoped-types-are-cheaper-than-global-types` provides the economic argument context that an agent consulting this spec may also need. No entry restates content already developed in the spec's body.
