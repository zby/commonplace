**Pass.** The two tables in this spec are reference lookup tables that cannot be replaced by prose without losing utility. The base-types table maps each type to its structural test and verifiability grade — exactly the information an agent or script needs to implement validation. The migration table maps old flat types to their new encodings — a one-to-one lookup that prose would make harder to scan. Both tables are information-dense and functional, not decorative.
