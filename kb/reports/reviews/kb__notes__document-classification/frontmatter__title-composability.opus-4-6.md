pass — "Document classification" functions as a concrete artifact name (a spec/reference document), which is an acceptable exception to the composability requirement for causal framing. It works naturally as a noun reference in prose: "see the document classification spec" or "according to document classification, `spec` requires Design/Implementation sections." The topical form is appropriate here.
