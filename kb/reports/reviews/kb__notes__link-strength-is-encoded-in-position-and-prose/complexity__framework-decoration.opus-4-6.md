Pass. The two tables (position-to-strength and prose-pattern-to-role-to-strength) are load-bearing classifications. The position table assigns ordinal strengths to five positional contexts with a justification column — the justification column carries information (why each strength level holds) that cannot be condensed to a single sentence. The prose-pattern table maps specific signal words to roles, which is the operational content an agent or script would use to implement inferred-strength parsing. Both tables encode actionable classifications rather than decorative taxonomies.
