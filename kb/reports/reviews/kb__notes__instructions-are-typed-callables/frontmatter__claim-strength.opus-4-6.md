fail. The title "instructions are typed callables" is a definitional reframing that reads more like a design decision or analogy than a contestable empirical claim. An informed person would not argue the opposite ("instructions are not typed callables") — they would instead question whether the callable analogy is useful or appropriate. The title would be stronger if it surfaced the non-obvious consequence, e.g., "instructions should declare document type signatures to enable early validation." The note's status is speculative, which is consistent with this being a seedling-level framing rather than a settled claim.
