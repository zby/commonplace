fail. Mild scope drift: the file title says "instructions are typed callables" but the H1 heading reads "Instructions are typed callables with document type signatures" — the body's actual focus is specifically on document type signatures for skills, not on the broader category of all instructions (tasks, workflows, etc.). The body establishes that skills should declare typed signatures over document types, which is a narrower claim than the file title implies. The H1 is better aligned with the body than the file title is.
