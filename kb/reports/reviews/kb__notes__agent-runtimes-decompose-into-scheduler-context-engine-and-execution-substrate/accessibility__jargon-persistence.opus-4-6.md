- "bounded calls" appears throughout the note (six or more times) as the fundamental unit: "bounded calls for judgment," "bounded call," "bounded context." The term is introduced in the opening bullet list without an inline definition: "Scheduler — owns control flow, decomposition, and state progression across bounded calls." The note links to [bounded-context orchestration model](./bounded-context-orchestration-model.md) for the formal treatment, but the link is not a definition. A reader encountering this note without that background cannot infer from the bullet list what "bounded" modifies. Recommend adding a brief gloss on first use, e.g., "bounded calls — individual LLM invocations with a fixed context window."

- "context engineering" is used throughout (in the component title "context engine" and in the body) and is flagged as a definition-linked term in the KB's own vocabulary. The note does link to [context engineering](./definitions/context-engineering.md). Per gate rules, terms defined in `kb/notes/definitions/` when the note links to the definition are excepted. This term is correctly handled and should not be flagged.
