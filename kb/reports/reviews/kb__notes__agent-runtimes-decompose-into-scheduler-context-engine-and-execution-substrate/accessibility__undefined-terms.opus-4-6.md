- "Ralph Loop" appears in the mapping table: "Long-horizon execution / Ralph Loop | Scheduler." The term "Ralph Loop" is used as a known concept without any inline explanation. A reader cannot tell from this table what the Ralph Loop is or where the name comes from. The note cites the source as "Anatomy of an Agent Harness," but the inline prose provides no gloss. Recommend adding a brief gloss in the table cell or prose, e.g., "Ralph Loop (the long-horizon execution loop described in the Anatomy of an Agent Harness source)."

- "DevTools Protocol" appears in the Relevant Notes section for the Harness Engineering ingest: "dynamic observability (DevTools Protocol wired into runtime) extends auto-injection from documents to runtime state." This is the browser DevTools Protocol repurposed for agent observability. A reader unfamiliar with this use may not know what it refers to. Since this appears only in the Relevant Notes section, not the main body, the impact is limited. Not a blocking finding.
