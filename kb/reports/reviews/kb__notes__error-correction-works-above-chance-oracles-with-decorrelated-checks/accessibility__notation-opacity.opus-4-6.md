The note uses several statistical notations that are defined within the note itself.

**"TPR" and "FPR"** — defined inline with formal probability notation: "TPR (true positive rate) = P(oracle says 'accept' | output is correct)" and "FPR (false positive rate) = P(oracle says 'accept' | output is incorrect)." Both are self-contained definitions. The subsequent uses, including the table and the formula "1/(TPR - FPR)²," are readable using these inline definitions. No opacity issue.

**"1/(TPR - FPR)²"** — this formula appears in: "The scaling law: the number of repetitions needed grows as 1/(TPR - FPR)²." The formula is immediately explained in plain prose ("We are separating two binomial distributions whose means differ by (TPR - FPR)..."). Self-contained. Acceptable.

**"Condorcet's jury theorem"** — referenced as a proper name for a classical result, with a link and inline description: "if each voter independently chooses correctly with probability p > 1/2, majority vote converges to the correct answer as the number of voters grows." Self-contained. Acceptable.

**"strength of weak learnability"** — referenced with a link: "The ML equivalent is strength of weak learnability — weak classifiers (barely above chance) can be boosted into arbitrarily accurate ones." The inline description makes it self-contained. Acceptable.

No notation from other notes appears in the prose without definition.

pass
