- "hard-oracle regime" — used in the closing paragraph ("Both sources operate in the hard-oracle regime (mechanically verifiable sub-step correctness)") with a parenthetical gloss. The gloss is adequate; no flag.

- "soft-oracle tasks" — the final sentence contrasts "hard-oracle" with "soft-oracle tasks — synthesis, creative work, ambiguous judgment." The gloss via the dash is adequate.

- "clean frames" — used in the heuristic "Exploit clean frames recursively" and again in the empirical section ("scoped recursion — pruning history to retain only direct dependencies"). The phrase "clean frames" is not defined inline, though its meaning is approximated by "pruning history to retain only direct dependencies" in the same paragraph. The first use ("Exploit clean frames recursively") stands without any definition or gloss. Readers who haven't read the linked orchestration model note won't know what a "clean frame" is.

Recommendation: Add a brief inline gloss on first use of "clean frames," e.g., "clean frames (local prompts that contain only the dependencies of the current step)."
