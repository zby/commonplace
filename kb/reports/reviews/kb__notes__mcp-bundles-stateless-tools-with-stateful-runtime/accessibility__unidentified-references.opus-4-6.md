"MCP" (Model Context Protocol) is expanded in the first sentence: "The Model Context Protocol packages two things together." No issue there.

"Claude Code" is mentioned as an example — "Claude Code's native tools already are — Read, Write, Grep, Bash are direct function calls." The note does not say what Claude Code is. A reader unfamiliar with it would not know whether it is a product, a framework, a codebase within this repo, or something else. The note should add a brief inline identification on first mention.
