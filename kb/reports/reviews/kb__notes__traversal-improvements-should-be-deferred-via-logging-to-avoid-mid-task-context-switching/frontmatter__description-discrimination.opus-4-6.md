**pass** The description adds the cost mechanism (loading writing methodology is expensive) and the specific solution (one-line log entry preserves signal at near-zero cost while a separate pass does the fix). This is more informative than the title alone, which only names the recommendation; the description explains why the recommendation holds and what the deferred path looks like.
