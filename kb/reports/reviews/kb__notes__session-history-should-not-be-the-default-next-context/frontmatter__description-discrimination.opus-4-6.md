pass. The description adds a mechanism the title leaves implicit: it names the two decisions that get conflated (storing execution history versus loading it), names the culprits (chat sessions and framework-owned tool loops), and characterizes the failure as making session history the default next context rather than an explicit choice. An agent scanning results would know this note is about a specific execution-boundary design pattern, not general advice about context management.
