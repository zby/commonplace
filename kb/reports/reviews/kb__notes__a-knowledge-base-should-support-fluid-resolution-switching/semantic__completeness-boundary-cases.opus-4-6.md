The note presents one framework: resolution-switching across abstraction levels, with four mechanisms (titles vs bodies, indexes vs notes, link semantics as zoom direction, progressive disclosure as resolution gradient) and an evaluative criterion (resolution-switching fluidity) with four concrete symptoms of failure.

---

**Framework: Resolution-switching mechanisms**

Grounding definition: "A knowledge base that supports good thinking must support this motion fluidly" — fluid movement between broad (landscape) and narrow (mechanism) abstraction levels.

- Simplest instance: a single note with a claim title. The title gives the broad view, the body gives the narrow view. Two resolutions, one switch. ✓
- Most extreme: a deep hierarchy of indexes → sub-indexes → notes → linked mechanism notes → linked foundations. Each hop is a resolution switch. The framework handles this through progressive disclosure as a gradient. ✓
- Between: a note that functions as both a detailed argument AND a navigational hub (e.g., an index with substantial prose). The reader's resolution is ambiguous — are they at the broad level (index) or the narrow level (content)? INFO — the framework treats resolution as a property of the artifact type (index = broad, note = narrow) but some artifacts straddle both roles. The note doesn't address dual-role artifacts.
- Adjacent concept — **temporal resolution**: switching between current state and historical state of knowledge (e.g., how a concept evolved). The note covers only spatial/abstraction resolution. This is outside the note's stated scope and doesn't need to be covered.
- The four symptoms of poor resolution-switching (no outbound links, bare-link indexes, topic-titled notes, missing relationship articulation) are all *deficiency* symptoms. There are no symptoms for the opposite failure: excessive resolution-switching that disorients (too many hops, too many abstraction levels to traverse before reaching actionable content). INFO — the symptom list is one-directional; a completeness-oriented framing would also identify over-fragmentation as a resolution-switching failure.

No WARN. Two INFOs: dual-role artifacts not addressed, and symptom list covers only deficiency not excess.
