INFO

Most grounding claims are well-aligned. Two are plausible inferences rather than direct grounding:

1. **Link semantics as zoom direction.** The note claims "Link semantics encode zoom direction" — "'Since [X]' zooms into a foundation... 'This extends [Y]' zooms out toward a generalization." The linked note (link-strength-is-encoded-in-position-and-prose) discusses relationship words in terms of roles (premise, evidence, elaboration) and strength signals, not zoom direction. Interpreting "since" as "zooms into a foundation" and "extends" as "zooms out" is this note's own reframing. The inference is plausible but the linked note doesn't make this claim.

2. **Resolution-switching enables discovery.** The note says "resolution-switching is the navigation skill that makes discovery possible — you can only see the particular as an instance of the general if you can move between the two levels." The discovery note discusses the dual structure of discovery (positing general, recognizing particular) but doesn't mention resolution-switching or navigation skill. The connection is this note's analytical contribution, not something grounded in the target.

Neither is scope overreach — both are reasonable extensions. But in both cases the note presents its own inference as if the linked note establishes it, when the linked note provides the raw material for the inference without making it explicitly.
