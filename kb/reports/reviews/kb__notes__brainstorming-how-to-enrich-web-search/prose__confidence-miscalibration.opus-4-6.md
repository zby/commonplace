## Gate: prose/confidence-miscalibration

**Result: PASS**

Frameworks and taxonomies in the note:

1. **Three abstraction depths** (shared feature / shared structure / generative model): Attributed to the discovery epistemology note via link. Properly sourced within the KB.

2. **Five-phase architecture** (Seed, Snapshot & inter-connect, Synthesize & redirect, Bridge, Report): The note's own construction. Framed under the heading "Proposed architecture" — the word "Proposed" calibrates correctly. Phases are described as design choices, not established methodology.

3. **Stopping criteria taxonomy** (diminishing returns, query exhaustion, budget, user checkpoint): The note's own analytical decomposition. Each criterion is explicitly labeled with its oracle type via parentheticals referencing the oracle-strength spectrum note. The classification proposes rather than asserts.

4. **"Naive deep search stays at level 1"** and **"/connect already operates at level 2"**: Analytical claims stated as fact. Both are well-grounded — the first follows from the definition of level 1 (shared features = embedding similarity), and the second is a factual claim about an existing skill the author built.

5. **"Aggressive pruning heuristics are needed"**: Assertive phrasing for a design opinion. In context ("Each iteration means more LLM calls ... N results x connection passes x iteration rounds") the necessity claim follows from the preceding analysis. Acceptable for a design exploration note.

No miscalibrated instances found. The note's own constructions are framed as proposals; sourced frameworks are attributed.
