**Fail.** Two entries are inflated. The `automating-kb-learning-is-an-open-problem` entry says "the boiling cauldron concept maps directly to the iterative search loop" — but the body already states this explicitly ("This is the boiling cauldron loop applied to search") with an inline link. The `scenario-decomposition-drives-architecture` entry appears twice (both entries carry identical semantics about "deep search defines a third scenario type"), which is a duplication error, and in both cases the body already says the MVP's five phases are "a scenario decomposition." These footer entries restate what is already explained inline.
