**pass** The title is a "why X despite Y" framing that implies the expected answer might be "don't use directories." An informed reader could argue for that position: flat files with good search infrastructure are preferable to directories, which create routing complexity, require registration in multiple config files, and force relative path arithmetic. The note directly addresses this by arguing directories extend the human-navigability guarantee by one to two orders of magnitude before the same problem recurs — a concrete scale argument, not just aesthetic preference.
