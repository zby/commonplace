**pass** The title makes a contestable claim. An informed reader could argue that bidirectional refactoring between neural and symbolic components is already possible without unified conventions — you just update the call sites when you change implementations, which is standard practice. The note addresses this by arguing that per-call-site updates create friction that discourages incremental refactoring and pushes toward big-bang rewrites, while unified conventions make each step local and coordination-free.
