**pass** The body directly supports the title. It explains the mechanism (name-based dispatch over a hybrid VM), shows how the unified convention makes each constraining/relaxing step local, and distinguishes the imperative scheduler approach from declarative DSLs. The note stays within the scope the title sets — architectural enablement of bidirectional refactoring — with no scope or claim drift.
