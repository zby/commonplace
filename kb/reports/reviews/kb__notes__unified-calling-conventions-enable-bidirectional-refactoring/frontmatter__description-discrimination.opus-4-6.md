**pass** The description adds the specific implementation mechanism (name-based dispatch over a hybrid VM), names the concrete system (llm-do), and specifies that components can move between neural and symbolic without changing call sites. This is substantially more informative than the title alone — a reader knows the note is about a specific architectural pattern, not just an abstract claim about refactoring.
