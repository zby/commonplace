- "affordances" — used repeatedly throughout the note ("structural affordances," "most structural affordances are directory-local," "affordances relevant across all directories," "thick affordances are already directory-local," "zero cost when elsewhere," "real global affordances"). The term appears ~8 times. It is never defined inline. In this KB's context, "affordances" means something like "the set of things an agent or user can do with or expect from a document type." Readers from outside the KB-design context may interpret it differently (from HCI: what an interface invites you to do, or colloquially: features/capabilities).

Recommendation: Add a brief gloss on first use, e.g., "structural affordances (the structural expectations an agent or user can rely on for a given document type)," then use the shorter form.

pass (for other terms — "maturity ladder," "resolution mechanism," and "progressive disclosure" are used but with enough surrounding context)
