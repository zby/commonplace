- "progressive disclosure" — used in the closing section: "This is progressive disclosure applied to the type system." The note does not define progressive disclosure. In UX/information architecture, progressive disclosure means revealing information incrementally as it becomes relevant. The note uses it with that meaning, but the term is assumed known.

Recommendation: Add a brief gloss: "progressive disclosure (presenting detail only when it becomes relevant) applied to the type system."

- "maturity ladder" — used twice: "thin global layer might be just the maturity ladder" and "The portable types are the thin global ones." The table underneath defines `text` (no frontmatter) and `note` (has frontmatter with description) as the two rungs, which provides context. Adequate pass.

- "ADR" — used in the body as "ADRs stay in `adr/`." This abbreviation appears without expansion. In this KB context ADR is an established abbreviation for Architecture Decision Record, and it appears in directory paths and the routing table in CLAUDE.md. Borderline; in an internal KB document this is acceptable if the full term appears elsewhere in context. However, this note never expands it.

Recommendation: Expand on first use: "ADRs (Architecture Decision Records) stay in `adr/`."
