**Fail.** The note contains two tables that are partially decorative. The "Where affordances actually live" table (four rows mapping structural expectations to their current home and what the global type says) communicates its central point — that the global type mostly says `note` and delivers nothing — and could be replaced by two sentences: "Most structural expectations (ADR sections, related-system review structure, source ingest fields) already live in directory conventions rather than the global type field. The global type for all of these is effectively `note`, which tells an agent nothing actionable." The table adds some clarity of enumeration but does not communicate meaningfully more than that sentence pair. The second table ("Global type → What it tells an agent") proposing a thin two-row global layer is similarly replaceable: "`text` means no frontmatter; `note` means has frontmatter with description — those two are the only globally useful types." The tables are mildly helpful for scannability but do not pass the test of communicating information that prose cannot.
