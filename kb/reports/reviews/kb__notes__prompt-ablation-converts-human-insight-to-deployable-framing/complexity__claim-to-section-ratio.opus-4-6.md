Pass. Six sections carry four distinct claims plus two reference sections: the pattern section defines the procedure (claim: ablation with a known target converts human insight to deployable framing); "Why it works" explains the oracle mechanism and the lossiness of conversion (two non-obvious sub-claims); "Design constraints" states what makes the pattern succeed or fail (three operational constraints); and "Open questions" extends without restating. The "Deployments" section is a reference, not a claim — it records where the pattern was applied and what it produced. The ratio is acceptable: the reference section does not inflate sections-per-claim beyond what the claims warrant.
