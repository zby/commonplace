pass — The title makes a contestable architectural claim: that distillation status (not compression level or loading frequency) is the right criterion for placing artifacts in `instructions/` vs `notes/`. Someone informed could reasonably argue the opposite — that loading frequency or artifact type (procedure vs. reasoning) is the better criterion, or that distillation status and directory placement should remain decoupled. The note explicitly frames it as a hunch to evaluate, not settled fact, which is appropriate for a seedling.
