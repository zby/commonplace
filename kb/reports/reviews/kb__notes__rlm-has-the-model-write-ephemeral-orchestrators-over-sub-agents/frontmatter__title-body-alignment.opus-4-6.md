pass. The body directly supports both parts of the title: "model writes orchestrators" is established in the "What RLM gets right" section (the model emits code as plan, code executes as orchestrator), and "ephemeral" is established in the "Ephemerality" section (orchestrators discarded after each run). The scope stays on RLM's architecture and its trade-off with repo-based learning, with no drift.
