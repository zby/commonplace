- "bounded contexts" / "bounded calls" is used throughout the note (six or more times) as the fundamental unit of the framework: "where the scheduler lives," "bounded contexts coordinate," "bounded execution." The term is never defined inline in this note. A reader who has not read the bounded-context orchestration model note cannot infer from this note alone what "bounded" means in this context. Recommend adding a brief gloss on first substantive use, e.g., "bounded contexts — individual LLM calls with fixed context windows."

- "clean model" / "degraded model" appears in the Scheduler Placement section: "This is the clean-model / degraded-model split." These KB-internal terms are introduced with a slash but no definition. The linked note ([LLM-mediated schedulers are a degraded variant of the clean model](./llm-mediated-schedulers-are-a-degraded-variant-of-the-clean-model.md)) presumably defines them, but the surrounding sentence does not say what makes a scheduler "clean" or "degraded." A reader encountering these terms for the first time cannot infer the meaning from context alone. Recommend a brief inline gloss, e.g., "the clean-model / degraded-model split: whether bookkeeping runs on exact symbolic state (clean) or inside a bounded stochastic LLM call (degraded)."
