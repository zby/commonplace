**Pass.** The eight Relevant Notes entries each link to systems or notes that occupy specific points in the design space. The body references most inline, but the footer adds navigational value by specifying which dimension each linked note addresses (e.g., "grounds: coordination forms vary independently of scheduler placement" for the conversation-vs-prompt-refinement link). An agent scanning the footer gets a structured map of where each prior note sits in the design space.
