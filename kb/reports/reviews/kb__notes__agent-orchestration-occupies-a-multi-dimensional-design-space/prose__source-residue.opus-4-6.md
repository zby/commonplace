PASS

The title claims generality over agent orchestration design. The body uses specific systems (RLM, Slate, forking) as examples to populate the design space, each explicitly framed as occupying a particular region. No single source's vocabulary dominates the framing. "Episodes," "thread-weaving," and "forking" are each attributed to their respective sources.