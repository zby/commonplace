- "Slate" is introduced in the Persistence Horizon section: "Slate's episodes retained across a task." The note does not say what Slate is — an agent framework, a product, a research prototype? The name appears twice in the body and once in Relevant Notes. The linked Relevant Notes entry says "Ingest: Slate: Moving Beyond ReAct and RLM," which provides some context (it's a system positioned relative to ReAct and RLM), but a link is not inline identification. Recommend adding a brief inline gloss on first mention, e.g., "Slate (an agent orchestration system with session-level episode management)."

- "RLM" is introduced in the Scheduler Placement section: "[RLM](./rlm-has-the-model-write-ephemeral-orchestrators-over-sub-agents.md) is a boundary case." The note does not say what RLM is or stands for inline — only that the linked note describes it. The acronym is unexpanded. Recommend expanding on first use, e.g., "RLM (Runtime Loop Management, a pattern where the model writes ephemeral orchestrators over sub-agents)." Note: do not fabricate the expansion — if the note's linked source expands the acronym, use that; if not, recommend the author add the expansion.

- "Forking" with a link to "[What Survives in Multi-Agent Systems](../sources/voooooogel-multi-agent-future.ingest.md)" — the note refers to "Forking" as if it is a named system or technique from that source. It is unclear whether this is the author's name, a technique name, or a feature name. The note says: "[Forking](../sources/voooooogel-multi-agent-future.ingest.md) changes coordination form without changing scheduler placement." The use of a capitalized proper-noun link for what might be a generic concept is ambiguous. Not a strong finding, but worth noting.
