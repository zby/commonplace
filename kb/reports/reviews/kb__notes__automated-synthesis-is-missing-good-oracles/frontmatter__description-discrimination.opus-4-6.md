pass — The description adds the key mechanism: generation is the easy part; the hard part is evaluation. This flips the implicit expectation (that generation would be the bottleneck) and adds implication (the system cannot run unsupervised). An agent could use this description to distinguish the note from notes about synthesis quality, synthesis techniques, or general automation limits — all related but different.
