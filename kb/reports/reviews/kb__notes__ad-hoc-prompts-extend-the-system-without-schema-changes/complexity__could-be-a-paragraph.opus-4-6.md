pass

Attempting a one-paragraph reduction:

> Any system with an LLM agent layer can absorb new requirements through natural language prompts without changing the deterministic base — because LLM context is homoiconic (instructions and content share the same representation), a markdown file is both content and executable instruction. Prompts sit at the loosest end of the enforcement gradient and resist premature formalization because they carry judgment a type signature can't express, making them effective sub-agent interfaces that define a lexically scoped frame. The maturation trajectory is: write ad hoc prompts first, extract to a skill when the same pattern recurs.

That paragraph captures the main argument. What it cannot hold without becoming unwieldy:
- The KB collections example, which concretely demonstrates *why* a prompt is better than a formal type (it carries context a schema couldn't).
- The Open Questions, which are worth surfacing.

The collections example is a genuine grounding contribution — the abstract claim "prompts carry what types can't" is much clearer after the concrete case. The note's section structure is justified by the presence of a demonstration that would be buried in a paragraph. The note does not fail this gate.
