PASS

Test frames:
- "since ad hoc prompts extend the system without schema changes..."
- "because ad hoc prompts extend the system without schema changes..."

These work cleanly as composable prose fragments. The title is concise and action-oriented — it names a subject (ad hoc prompts), a verb (extend), and a distinguishing condition (without schema changes). No awkward grammar.
