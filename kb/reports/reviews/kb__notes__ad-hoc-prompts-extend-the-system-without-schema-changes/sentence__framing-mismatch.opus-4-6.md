INFO — 1 instance

**Line 39:** "This matters most for sub-agent handoff."

The sentence frames the value of a clean context boundary ("the caller does the judgment-heavy work and writes it down") as primarily a sub-agent handoff concern. But the mechanism — separating judgment from execution by writing it down — applies more broadly: any situation where a caller passes a task to a separate process (a human, a script, a later agent invocation) benefits from the same clean-boundary discipline. The "sub-agent handoff" framing is accurate but narrower than the actual mechanism.

**Severity:** INFO. The sentence emphasizes sub-agent handoff without explicitly excluding other contexts. The framing does not actively mislead — sub-agent handoff is the paradigm case in this note — but a reader might not notice the mechanism generalizes.
