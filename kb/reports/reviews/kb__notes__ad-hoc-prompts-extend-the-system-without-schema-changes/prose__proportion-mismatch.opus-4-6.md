PASS

The core claim (ad hoc prompts extend the system without schema changes) receives proportional development across four sections:

- The KB example section makes the claim concrete with a worked case.
- "The constraining spectrum" positions the claim within the broader enforcement gradient and explains when to use ad hoc vs. typed approaches.
- "Prompts carry what types can't" explains the expressive advantage.
- "Why this works: homoiconicity" provides the mechanism.

The mechanism section is shorter than the others, but as a closing explanatory section this is appropriate — it answers "why" concisely after the "what" and "when" have been established. No section is overdeveloped relative to the core claim.
