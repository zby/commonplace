INFO

**"Why this works: homoiconicity" — borderline restatement in the opening sentence.**

The section opens: "Ad hoc extension without schema changes is possible because the LLM context is a homoiconic medium." The note's core claim ("ad hoc prompts extend the system without schema changes") was already established in the title and the introductory paragraph. This opening restates the claim before pivoting to the mechanism.

The restatement is one sentence and functions as a transition rather than a full re-explanation, so this is INFO rather than WARN. The section works from its second sentence onward without it ("— instructions and content share the same representation..."), but the first sentence is a natural setup for the causal claim that follows.

No other sections open with restatements. The KB example, constraining spectrum, and prompts-carry sections all open on fresh content.
