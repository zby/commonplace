INFO

One adjacent section pair has a specific case preceding a more general framework section.

**Pair: `## The KB example: collections` → `## The constraining spectrum`**

`## The KB example: collections` is a specific case — a single concrete instance (the collections requirement in the KB) demonstrating ad hoc extension. The section is framed explicitly as an example: "We first noticed this in the KB, where it shows up cleanly."

`## The constraining spectrum` is a general framework — it places ad hoc prompts on the enforcement gradient (instructions → skills → hooks → scripts) and establishes the maturation trajectory from ad hoc to extracted skill. This is a general principle that applies across all agent systems, not just the KB.

**Tiebreaker assessment:** The KB collections example illustrates the title claim ("ad hoc prompts extend the system without schema changes"), which is also the claim stated in the intro paragraphs. The constraining spectrum is a *different* general frame (where prompts sit on the enforcement gradient) rather than the general rule the KB example directly exemplifies. The KB example doesn't specifically ground the constraining spectrum section. This makes the ordering defensible — the specific example illustrates the intro's claim, and the constraining spectrum follows as a new conceptual frame.

**Why INFO rather than WARN:** The general rule the KB example exemplifies (intro paragraphs) precedes it. The adjacent general section (`## The constraining spectrum`) is a different conceptual frame, not the general rule that `## The KB example` is an instance of. The reader gets the general claim before the specific case; the constraining spectrum follows as an extension, not as the rule the example was demonstrating. The ordering is defensible, but worth reviewing if the goal is to lead with all general frameworks before any specific illustration.
