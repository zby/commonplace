pass

Sections: 4 content H2s (The KB example, The constraining spectrum, Prompts carry what types can't, Why this works: homoiconicity) plus Open Questions. Intro paragraphs carry 2 claims before any heading.

Distinct non-obvious claims:
1. Any LLM-agent-layer system absorbs new requirements through prompts without changing the deterministic base.
2. A prompt describing context (e.g., a collection) is better than a formal type because it carries judgment a schema can't.
3. Ad hoc prompts sit at the loosest end of the enforcement gradient; the maturation trajectory is ad hoc → notice pattern → extract skill.
4. Prompts carry the caller's judgment (not just data), which is why they resist premature formalization.
5. Prompts are effective sub-agent interfaces because they define a lexically scoped frame.
6. Ad hoc extension without schema changes works because LLM context is homoiconic.

Six claims across five sections (excluding Open Questions). The ratio is healthy. "The KB example: collections" is illustrative rather than a new claim — it grounds claim 2 with a concrete case rather than extending the argument. That is legitimate use of a section. "Open Questions" is a holding area, not a claim-bearing section.
