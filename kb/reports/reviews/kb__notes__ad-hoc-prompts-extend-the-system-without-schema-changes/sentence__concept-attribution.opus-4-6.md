PASS

Two attribution claims checked against their targets:

1. Line 35 — "This is [lowest-friction capture, then progressive refinement](./wikiwiki-principle-lowest-friction-capture-then-progressive-refinement.md) applied to the skill layer." The linked note's filename encodes the concept directly: "wikiwiki-principle-lowest-friction-capture-then-progressive-refinement." The attribution is structural — the note names this principle — so the identity claim is consistent with the target. ✓

2. Line 41 — "The prompt defines what's visible in the sub-agent's [lexically scoped frame](./llm-context-is-composed-without-scoping.md)." Checked the target. The scoping note uses "lexically scoped frame" as its term for sub-agent isolation: "This is lexical scoping: the sub-agent's 'code' (its prompt) determines what's visible, not the runtime history." And: "Lexically scoped (frame-local): The sub-agent's system prompt, the specific input for this invocation, any context the caller explicitly passes." The link text and the target's usage align exactly. ✓

Three other links not checked (attribution test limited to identity claims in prose; remaining links are in "Relevant Notes" section which states relationships, not identity).
