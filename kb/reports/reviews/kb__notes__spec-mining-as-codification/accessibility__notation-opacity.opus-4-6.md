"TPR > FPR" appears twice — "create an oracle with TPR > FPR" and "mined specs create oracles with TPR > FPR" — without expansion. These stand for True Positive Rate and False Positive Rate, originating from the oracle-strength-spectrum note. The sentence "mine a spec (create an oracle with TPR > FPR), then amplify through decorrelated repetition" only makes sense if the reader already knows these abbreviations. Since the note does not use them in formal derivations, replace with plain language: "create an oracle that detects real problems more often than it raises false alarms" or similar.
