fail. The description ("Link-following is local with context; search is long-range with titles/descriptions; indexes bridge both modes") essentially summarizes the body structure rather than adding mechanism, implication, or context beyond the title. An agent reading the title already expects the note to enumerate the different requirements for each mode; the description confirms this but adds no new retrieval signal — it is a compressed restatement, not a discriminating addition.
