pass. "Quality signals for KB evaluation" works as a concrete artifact name (a named catalogue), which is an acceptable composability form. It also slots into prose naturally: "the quality signals for KB evaluation include orphan rate, description uniqueness, and pruning accuracy" is fluent, and links like "see quality signals for KB evaluation" or "as the quality signals for KB evaluation show" are idiomatic.
