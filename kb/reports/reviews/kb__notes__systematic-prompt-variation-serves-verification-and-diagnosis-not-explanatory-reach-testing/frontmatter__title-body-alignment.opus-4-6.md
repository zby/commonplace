**pass** The body delivers exactly what the title claims. It defines verification and diagnosis as the two valid uses of prompt variation, distinguishes Deutsch's reach test as operating on a different object (the explanation rather than the prompt), and includes a summary table that maps each operation to what it varies and what success means. There is no scope drift; the prompt ablation section is correctly positioned as adjacent but distinct, not a counterexample to the title.
