**"Ephemeral"** / **"ephemeral"** — the note's central term. Introduced and defined in the first sentence of "The fork" section: "Ephemeral systems generate an artifact, use it, and discard it." Used subsequently throughout in "What ephemerality buys," "What ephemerality costs," and "Ephemerality as anti-codification." Every use remains grounded by the definition and the contrast with "accumulating." The term is standard-enough in this context (it carries its plain-language meaning) and is re-anchored in each section header. No persistence issue.

**"Codification"** — introduced as a link in the "Ephemerality as anti-codification" section: "[Codification](./definitions/codification.md) converts stochastic LLM behavior into deterministic, testable artifacts." This is a definitions-linked term. The link serves as the definition point per the gate's exception rule. The inline description "converts stochastic LLM behavior into deterministic, testable artifacts" also provides a self-contained gloss. Acceptable.

**"codification-relaxing dynamic"** — appears once in "The [codification-relaxing dynamic](./codification-and-relaxing-navigate-the-bitter-lesson-boundary.md) is about where to place each component on this spectrum." The phrase is used once and the surrounding context ("where to place each component on this spectrum") grounds it sufficiently. No persistence issue.

**"RLMs" (Recursive Language Models)** — defined inline: "Recursive Language Models (RLMs) work this way." Appears only once. Acceptable.

pass
