pass. The body supports the title: it identifies what a Claw does beyond retrieval (four action modes), explains what changes for the learning loop, and explicitly frames the retrieval view as "not wrong, just partial." There is no scope or claim drift. Note: this note and `claw-learning-loops-must-improve-action-capacity-not-just-retrieval` cover substantially overlapping content — the body here mostly repeats the argument of the other note with less detail.
