pass. The description adds the specific diagnostic context (the KB's structured-reasoning cluster conflates the two), the empirical grounding (agentic-code-reasoning evidence), and the directional implication (process constraints drive accuracy gains where output format alone would not). These three additions make the description substantively different from the title and help distinguish this from generic "prompt structure" or "output format" notes.
