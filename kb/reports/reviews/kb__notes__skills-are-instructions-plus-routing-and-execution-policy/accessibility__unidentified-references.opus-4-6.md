Two agent platforms are mentioned without inline identification:

1. "Codex" appears at lines 19, 56, 59-60 (e.g. "Codex from `.agents/skills/`") without stating what it is. The name "Codex" is ambiguous — it could refer to OpenAI's original code-generation model or the newer Codex CLI agent platform. Suggest identifying on first mention, e.g. "Codex (OpenAI's CLI agent platform)."

2. "Cursor" appears at lines 56, 61 (e.g. "Cursor from `.cursor/skills/`") without stating what it is. Suggest identifying on first mention, e.g. "Cursor (an AI coding assistant)."