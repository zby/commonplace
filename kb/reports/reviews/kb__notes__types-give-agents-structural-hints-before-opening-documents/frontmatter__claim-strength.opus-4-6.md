**pass** The title makes a contestable claim. An informed reader could argue the opposite: that embedding-based semantic search makes pre-read structural hints unnecessary — agents can retrieve relevant documents based on content similarity without inspecting type or description metadata first. The note addresses this implicitly by grounding the argument in the constraint that agents are stateless and context is finite, making the per-document loading cost the operative constraint, not retrieval accuracy.
