**pass** The description adds the two-layer mechanism (type says what operations a document affords, description filters among instances of that type) and names the specific decision enabled (routing without loading full documents). This tells a reader more than the title alone — in particular that type and description serve distinct filtering functions rather than being redundant alternatives.
