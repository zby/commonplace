fail. The title is very long and forces unwieldy prose: "since reverse-compression (inflation) is the failure mode where LLM output expands without adding information, we need epiplexity as the measure..." is grammatically valid but heavy enough to disrupt reading flow. The parenthetical "(inflation)" compounds the problem. The short-form name "reverse-compression" works well as an inline reference, and other notes in the KB already use it that way — the full title is the artifact name, but it's too long for clean prose embedding.
