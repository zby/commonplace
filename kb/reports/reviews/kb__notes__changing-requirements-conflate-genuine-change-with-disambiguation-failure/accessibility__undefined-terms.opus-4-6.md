- "projection" is used as a technical term without inline definition: "B's author performed a **projection** — collapsing the space of valid interpretations to one." The dash-clause that follows does define it, so this is borderline. However, the term is also used bare later: "the spec-to-program projection model" assumes the reader already accepts "projection" as established vocabulary. The first use is adequately glossed; no flag needed.

- "deploy-time learning" is introduced in the section heading "Implications for deploy-time learning" and then used throughout that section without definition. The link to `deploy-time-learning-is-agile-for-human-ai-systems.md` is present but a link is not a definition. First sentence of the section: "Deploy-time learning extends agile's loop to human-AI systems" — this gives partial context (it extends agile's loop) but does not say what deploy-time learning *is*. A brief gloss ("deploy-time learning — the practice of iterating on a system after it is in production") would serve a reader unfamiliar with the term.
