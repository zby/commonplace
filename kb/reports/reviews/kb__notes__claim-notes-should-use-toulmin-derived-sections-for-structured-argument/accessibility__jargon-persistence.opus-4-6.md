- "constraining pattern" is introduced in the Reasoning section: "This is the [constraining pattern](./definitions/constraining.md) applied to the type system." It appears only once in the body, so no persistence problem here.

- "bounded semantic checks" appears in the section template block: "bounded semantic checks verify the content within each section." It then reappears in "Each of these is a bounded judgment within a known section, not an open-ended document read." The term "bounded" as used here carries KB-specific meaning (a bounded context / bounded LLM call), but both uses appear close together and the second paraphrase ("a bounded judgment") is sufficiently self-explanatory in context. No flag.

- "hard-oracle" is used once in the section template discussion ("the section-presence checks are hard-oracle") without definition. It appears only once, so jargon persistence is not triggered, but see undefined-terms gate.
