"TPR" and "FPR" appear without expansion in the Amplify section: "works whenever the oracle has discriminative power (TPR > FPR) and checks are decorrelated -- the cost scales with 1/(TPR-FPR)^2, so even a weak spec is useful" and later "models struggle to achieve TPR > FPR through introspection alone." These abbreviations come from the error-correction note and are not defined here. A reader unfamiliar with the notation must leave this note to decode the formula. Suggested fix: expand on first use, e.g. "true-positive rate (TPR) exceeds false-positive rate (FPR)" or replace with plain language: "works whenever the oracle is right more often than it is wrong."

Secondary: "(p,delta,k)-satisfaction" appears in the Relevant Notes annotation for ABC without definition. This is lower-stakes since it's in the link annotation rather than the body, but a reader scanning the links still cannot decode it. Suggested fix: replace with a plain-language description in the annotation, e.g. "parameterizes oracle strength per constraint via probability, tolerance, and sample-size thresholds."