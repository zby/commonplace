- "double-loop learning" — used in the Related Work section: "double-loop learning: organizations adapt by revising governing assumptions, not just actions." The parenthetical after the colon adequately glosses the term. Pass.

- "SECI spiral" — used in the Related Work section: "the SECI spiral: tacit → explicit → combination → internalization." The inline expansion gives enough structure to understand the reference without following any link. Pass.

- "LLMOps" — used without definition: "The individual practices — prompt versioning, eval-driven development, CI-gated prompt testing — are established in LLMOps." The note does not define or gloss LLMOps. While practitioners will recognize it, readers from adjacent fields (organizational learning, software engineering) may not know it refers to the operational practice of managing LLM-based systems in production.

- "semantic underspecification" — used in the examples section ("the constrained version, `python-slugify` commits to one interpretation, in code, once — resolving the underspecification permanently"). In this note the term is "underspecification" (not "semantic underspecification"), and the surrounding text ("a spec that looks precise but actually admits multiple valid interpretations") provides adequate context. Pass.

Recommendation: Add a brief gloss for "LLMOps," e.g., "LLMOps (the operational practice of deploying and maintaining LLM-based systems)."
