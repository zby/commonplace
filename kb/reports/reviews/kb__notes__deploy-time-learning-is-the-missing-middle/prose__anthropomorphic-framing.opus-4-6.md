PASS

No anthropomorphic language attributed to models. "The deployed system's behavior improves because its artifacts improve" attributes improvement to the system (a composite including human-maintained artifacts), not to the model itself. "AI researchers, trained to think about adaptation in terms of weights and gradients, tend to look past it" attributes cognition to humans, not models.