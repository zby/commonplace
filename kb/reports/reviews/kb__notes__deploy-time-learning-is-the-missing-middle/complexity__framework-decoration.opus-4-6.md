**Fail.** The "Three Timescales" table (Training / In-context / Deploy-time) can be substantially replaced by two sentences: "Deploy-time learning sits between training (durable but opaque, requires pipeline) and in-context (inspectable but ephemeral), using symbolic artifacts that are durable, inspectable, and verifiable across sessions." The table's four columns (When, Substrate, Properties) mostly restate what the surrounding prose already explains. Similarly, the pitchdeck progression table in "Concrete Examples" lists four example directories against "What moved to code" but the content of each row is covered by the inline prose description that follows. The verifiability gradient table is the most load-bearing of the three — its three-column breakdown (Resettable / Efficient / Rewardable) encodes information that would be awkward to express without the grid — but the timescales table and pitchdeck table are decorative rather than structurally necessary.
