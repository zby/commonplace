- "constraining" — introduced with a link to the definition ("constraining (narrowing the interpretation space)") with an inline gloss on first use. It appears ~6 more times in the body (verifiability gradient table, "constrain/relax cycle," "premature codification," "constraining revises the rules," "constraining at different grades"). All later uses retain adequate context from the first grounded use and the table. Pass.

- "distillation" — introduced with a link to the definition ("distillation (extracting procedures from reasoning)") with an inline gloss. Appears again in "Related Work" ("Constraining and distillation map to externalization and combination phases"). The second use is in a comparative context where the meaning is still recoverable. Pass.

- "codification" — introduced inline ("Codification is the far end of constraining, where prompts undergo a phase transition to deterministic code"). Used again several times in the examples and failure modes sections with consistent meaning. Pass.

- "hard-oracle regime" and "soft-oracle" — not present in this note.

- "verifiability gradient" — introduced as a section heading and explained thoroughly in the table that follows. Used again in "Related Work" and the closing paragraph. All uses remain grounded. Pass.

pass
