Pass. The note cannot be collapsed to one paragraph without losing the boundary conditions (when indirection should and should not be eliminated) and the KB example that makes the principle concrete and actionable for this specific system. The boundary section's three conditions are non-obvious — particularly that runtime parameters must stay variable while setup-time values should be baked in — and the test ("if you could do a find-and-replace before the LLM sees the text...") is a practical heuristic that would be lost. The sections are justified.
