**pass** The body directly supports the title's claim: it explains the different cost model for LLM instructions vs code, identifies three specific indirection patterns that become expensive (config variables, abstraction layers, conditional paths), gives a boundary condition for when elimination applies, and provides a concrete example (KB skill portability). No claim or scope drift detected.
