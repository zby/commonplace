**pass** The description adds a precise contrastive mechanism (in code, indirection is nearly free at runtime; in LLM instructions, every layer costs context and interpretation overhead on every read). This is materially more informative than the title — it names the comparison class (code), quantifies the difference ("nearly free" vs "every read"), and identifies the two cost dimensions (context and interpretation overhead).
