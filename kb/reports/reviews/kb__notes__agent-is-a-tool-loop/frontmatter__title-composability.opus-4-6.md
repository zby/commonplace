FAIL

Test frames:
- "since 'agent' is a useful technical convention, not a definition..."
- "because 'agent' is a useful technical convention, not a definition..."

Both are awkward as composable prose fragments. The core problem is the negation ("not a definition"): it makes the title contrastive/declarative rather than propositional. As a link fragment, you'd want to invoke the positive content — what an agent IS — not the negative framing of what it ISN'T.

Compare the filename alternative `agent is a tool loop`:
- "since agent is a tool loop..." — works cleanly
- "building on agent-as-tool-loop..." — works as a noun phrase
- "[agent is a tool loop](./agent-is-a-tool-loop.md), spawning a sub-agent is spawning a sub-loop" — composes naturally

The current H1 would require callers to either paraphrase ("since the tool-loop convention...") or use an awkward negation-framing ("since agent is a convention not a definition, spawning a sub-agent is..."). Neither is natural in-prose usage.

The filename `agent-is-a-tool-loop` is more composable than the current H1. Aligning the H1 to the filename would resolve this.
