CLAIM DRIFT — the title foregrounds a meta-claim; the body's most actionable content is the convention itself.

The title asserts: `"Agent" is a useful technical convention, not a definition` — a claim about the epistemological status of the word "agent."

The body's most concrete contribution is different: **agent = tool loop (prompt, capability surface, stop condition)**. The body then derives consequences: sub-agents are child loops, multi-agent systems are trees of loops, framework design should expose the loop as a first-class operation.

The title's "not a definition" is technically upheld (the body frames the equivalence as a convention, not a formal definition), but it's the least interesting thing the body establishes. The body is primarily constructive — it builds and uses the convention — and only briefly justifies treating it as a convention rather than a definition.

A second issue: the filename `agent-is-a-tool-loop.md` preserves the original claim title, which is stronger and more specific than the current H1. The current H1 appears to have drifted toward the meta-claim while the filename held the constructive claim.

**What the body actually establishes**: agent = tool loop with three components; sub-agents and multi-agent systems are natural extensions; framework design should treat sub-agents as first-class. The meta-claim ("convention, not definition") is supporting context, not the primary finding.
