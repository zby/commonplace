pass

The note has 3 footer links.

- `tool-loop-index.md` — body links inline ("the question of whether frameworks should expose the loop") with the same link target. The footer's label "context: the index whose argument this convention grounds — sub-agents as sub-loops" adds a relationship signal (grounds) not stated in the inline use. Borderline, but the directional label is useful for navigation.
- `bounded-context-orchestration-model` — not linked in the body at all. Footer reveals that each agent is one iteration of the select/call/absorb loop — a non-obvious connection that adds genuine navigational value.
- `subtasks-that-need-different-tools-force-loop-exposure-in-agent-frameworks` — not linked in the body. Footer explains why sub-agents are needed (children need different capability surfaces) — a non-obvious "motivates" connection.

All three entries add information not fully covered by the body or add clear directional relationship labels. No inflation.
