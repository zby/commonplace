## PASS — proportions are appropriate

The core claim: when frameworks offer only tools, codified scheduling patterns end up hidden inside those tools.

Development breakdown:
- **Setup** (paragraphs 1–3): Establishes that many codified patterns fit naturally in tools, including multi-step ones. ~3 short paragraphs.
- **Core distinction and consequence** (paragraphs 4–6 + bullet list): Scheduling patterns are different; embedding them in tools creates hidden schedulers; the fix is explicit control logic. ~3 paragraphs plus a four-item example list.

The load-bearing material (the scheduling/capability distinction and the hidden-scheduler consequence) receives at least as much development as the setup. The setup is appropriately brief — it exists to establish the baseline that the core claim contrasts against.

No proportion mismatch.
