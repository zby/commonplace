pass. The title is contestable — someone could argue that embedding scheduling logic in tools is fine or even preferable (simpler interface, single responsibility in practice), and that "hidden scheduler" is a pejorative framing for a legitimately useful pattern. The note takes a clear position that scheduling inside a tool is a misplacement of control logic once it has stabilized.
