pass. The description adds the mechanism (deterministic next-step policies need explicit control logic as behavior matures), the forcing condition (framework offers only tools), and the consequence (scheduling patterns end up there). This distinguishes the note from related notes about stateful tools and tool loops, and specifies the maturity-driven dynamic that the title alone does not convey.
