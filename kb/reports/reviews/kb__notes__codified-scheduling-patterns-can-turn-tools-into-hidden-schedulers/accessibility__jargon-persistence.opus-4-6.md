- "codify" / "codified" is used throughout: "codify into deterministic procedure," "Many codified patterns fit naturally," "A deterministic multi-step implementation can still be an ordinary tool," "Scheduling patterns are different," "codified scheduling patterns," "codified policy," "Once next-step policy has stabilized enough to become code." The note links "codify" to its definition on first use. All subsequent uses remain legible — "codify" is explained by the linked definition and the surrounding sentences ("deterministic procedure," "code"). No flag.

- "hidden scheduler" is introduced in the title and first used in the body: "it stops acting like an ordinary capability and starts acting like a hidden scheduler." This is a KB-internal concept but it is defined inline by the sentences that precede it: a tool that "decides which subtask to run, which tool surface to expose, when to recurse, and when to stop is not a tool — it is an orchestration runtime behind a tool-shaped facade." The definition is clear. "Hidden scheduler" appears in the Relevant Notes links but not again in the body. No flag.

- "tool surface" is used twice: "which tool surface to expose" and "alter the next tool surface." No inline definition is given. See undefined-terms gate.
