**pass** The description adds mechanism and scope beyond the title. It specifies how traversal becomes reasoning (following links reads as a chain of reasoning, the file tree becomes a scan of arguments) and names the grammatical mechanism (link semantics like "since, because" encode relationship types). This distinguishes the note from a generic "title conventions matter" note by grounding the benefit in a specific structural consequence.
