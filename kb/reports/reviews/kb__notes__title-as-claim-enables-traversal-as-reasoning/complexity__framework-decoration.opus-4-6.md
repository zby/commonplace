**Pass.** The three-row table ("Document kind / Title convention / Link semantics / Role in traversal") is load-bearing. It distinguishes single-claim, multi-claim, and definitional document types across four properties in a way that requires three sentences if written as prose and loses parallel visibility. The table encodes a typological claim (the three types are structurally different along all four dimensions simultaneously) that would require a more complex prose structure to communicate. It cannot be deleted and replaced by one sentence without loss.
