---
gate: prose/confidence-miscalibration
verdict: warn
---

The note's frontmatter marks it `status: speculative`, which is appropriate. Most analytical frameworks are hedged or explicitly labeled as hypotheses. Two instances assert where the note's own status calls for proposing:

1. **"The hierarchy becomes"** (Reasoning, paragraph 3) — presents the four-tier loading hierarchy as a result rather than a proposed model. The hierarchy is the note's own construction, extending a principle from another note. Suggest: "The hierarchy would become" or "Under this model, the hierarchy becomes."

2. **"Straightforward to build but impossible on someone else's runtime"** (Caveats) — "straightforward to build" is an unqualified engineering confidence claim. The note doesn't ground this estimate. The "impossible on someone else's runtime" half is well-grounded (the caveat explains why). Suggest: "Feasible to build in a custom runtime, but impossible on someone else's."

The hypothesis table is properly calibrated — the note explicitly says "Each row is a hypothesis." The definition bullets use assertive language ("They're small," "They're stable") but these describe observable properties of existing notes, not unsupported claims.
