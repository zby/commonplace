---
gate: prose/unbridged-cross-domain
verdict: pass
---

No unbridged cross-domain transfers found.

All cited evidence comes from the same domain (agent-operated knowledge bases). The linked notes are internal KB notes about agent design, document types, and loading hierarchies — all within the note's argument domain.

The one cross-domain reference is the programming-language analogy: "behave like imported constants in a programming language: declared once, available everywhere in scope." This is framed as analogy ("behave like"), not as evidence. The note does not claim that programming-language scoping rules prove anything about agent context injection — it uses the analogy to clarify the intended behavior. No bridge is needed for an explicitly labeled analogy.

The Harness Engineering external source is cited as extending the same domain (agent runtime engineering), not transferring from a different one.
