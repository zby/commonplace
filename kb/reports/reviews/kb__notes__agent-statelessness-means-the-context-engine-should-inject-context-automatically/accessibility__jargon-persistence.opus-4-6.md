- "context engine" is the central term of this note. It is introduced in the opening paragraph: "The remedy is automatic context injection: when the context engine loads a document..." The term is used nine or more times across the note body. "Context engine" is a KB-internal component term. The note links to the parent note on agent statelessness but does not define what a context engine is inline. A reader who arrives at this note directly cannot tell from the opening sentence whether the context engine is a specific piece of software, a conceptual component, or something else. Recommend adding a brief gloss on first use, e.g., "the context engine (the runtime component responsible for deciding what enters each LLM call)."

- "document affordances" appears once: "This extends [document affordances](./document-types-should-be-verifiable.md) from 'what operations can I perform on this document' to 'what context gets loaded alongside this document.'" The link partially explains the source of the concept, and the inline quote provides a functional gloss. Adequate as written. Pass.
