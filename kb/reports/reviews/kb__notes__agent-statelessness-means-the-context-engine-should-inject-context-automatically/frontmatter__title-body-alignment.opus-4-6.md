pass. The title claims statelessness implies the context engine should inject context automatically. The body delivers this: it opens with the statelessness mechanism (agents can't carry definitions between reads), argues the remedy is automatic injection, uses definitions as the cleanest motivating case, extends to other injection candidates (area indexes, ADRs, specs), situates this in the instruction specificity hierarchy as a new "on reference" layer, and enumerates caveats about runtime requirements and context budget. The claim is structural and the body supports it throughout.
