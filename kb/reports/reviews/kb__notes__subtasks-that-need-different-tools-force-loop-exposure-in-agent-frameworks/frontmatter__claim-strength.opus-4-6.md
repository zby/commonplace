**pass** The title makes a contestable structural claim: that different tool surfaces *force* loop exposure, not merely suggest it. An informed reader could argue that dynamic tool injection, meta-tools, or capability masks let a single loop serve heterogeneous subtasks without exposing the loop construct. The body then closes that escape by showing each workaround degrades to a hidden scheduler or leaks irrelevant affordances — making the "force" language earn its keep.
