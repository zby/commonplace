"bounded call" is used in the body multiple times after its implicit grounding through the Relevant Notes link. In the body: "Changing the capability surface changes the action alphabet of the next bounded call" — this is the third-level use. It is also used in the Relevant Notes section ("each child task is naturally a fresh bounded call"). The term is KB-specific (from the bounded-context orchestration model) and is never defined inline. However the note's use is limited enough (two body appearances plus the Relevant Notes entry) that a reader can track it contextually. No clear over-persistence failure, but the first body use ("the next bounded call") would benefit from a brief gloss like "the next isolated LLM call" since the note never defines the term.