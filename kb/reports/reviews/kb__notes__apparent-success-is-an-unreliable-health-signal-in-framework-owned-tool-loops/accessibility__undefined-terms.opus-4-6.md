"framework-owned tool loop" is used repeatedly throughout the note beginning in the opening sentence and title, but is never defined inline. On first encounter, the surrounding sentence does not paraphrase it or give enough context to infer its meaning beyond "a loop owned by a framework." The term "tool loop" is KB-specific vocabulary (linked via `./tool-loop-index.md`), but the compound phrase "framework-owned tool loop" — which is central to the note's claim — is not the same thing as "tool loop" and is never glossed. A reader new to the note cannot tell from the opening sentence what distinguishes a "framework-owned" loop from any other kind.

Suggested fix: add a brief inline gloss on first use, e.g., "In a framework-owned tool loop — an agent execution loop where the framework, not the application, controls error handling and recovery — a task can end in visible success even when..."
