pass. The claim is contestable: one could reasonably argue that inclusion decisions should be made at write time by the human author, not by an agent consulting an always-loaded goals statement — and that goals belong in documentation rather than the control-plane file. The title also implies that placing goals in always-loaded context is specifically what enables inclusion decisions, which is a non-obvious design choice others could dispute.
