pass. The title makes a contestable claim: that orchestration architectures need coordination *guarantees*, not just coordination *channels*. A knowledgeable practitioner could argue the opposite — that channels (conversation, shared memory, forking) are the primary architectural concern and that guarantees are an implementation detail, or that channels implicitly define the guarantee. The "not just" framing sharpens it: the claim is that channels are insufficient, which is a position someone could dispute.
