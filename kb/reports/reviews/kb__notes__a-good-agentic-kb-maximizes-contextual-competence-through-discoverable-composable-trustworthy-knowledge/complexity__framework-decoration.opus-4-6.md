warn

The note contains one table (lines 48–55) mapping five operations to capacity trade-off, primary property improved, and a Commonplace example.

**Test applied:** Can the table be deleted and replaced with one sentence without losing meaningful information?

One-sentence replacement: "Accumulation adds knowledge (reach varies), constraining trades generality for trustworthiness, distillation trades generality for discoverability, discovery creates generality for composability, and pruning removes knowledge for trustworthiness."

That sentence carries the mapping. The examples column (e.g., "text → note → structured claim → validation script") are already in the preceding paragraphs. The capacity trade-off column ("Generality → compound") is stated inline in the operations section.

The table reorganizes prose that immediately precedes it — none of the table's cells introduce information not in the paragraphs above. It functions as a summary/quick-reference rather than a carrier of new content.

**Verdict:** The table is decorative by this gate's definition. It provides scannable compression, which has value, but the information is fully in the prose. Replacing it with the sentence above (or removing it in favor of the existing summary section "The theory, compressed") would not lose meaningful content.
