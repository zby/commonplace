INFO

The note's three-properties and five-operations taxonomy is its own analytical construction, and the dependency-structure claims are stated as flat requirements rather than proposed frameworks:

- "For knowledge to improve an agent's actions under bounded context, it needs three properties." — Asserts necessity. "One useful decomposition is..." would propose.
- "Composability depends on the other two: you can't compose what you can't find (discoverability), and you can't build on an unreliable premise (trustworthiness)." — Asserts a dependency graph as if derived, without acknowledging it is the note's own model.
- "Discoverability is the foundation — without it, the other two are inert." — Asserts primacy.
- "Constraining primarily improves trustworthiness... Distillation primarily improves discoverability..." — The table and surrounding text assign primary/secondary property improvements with no hedging.

Per the gate's exception, qualitative decompositions are the note's own analytical moves and do not require sourcing — but hedging like "at least" or "one way to model this" is expected. These instances use assertive grammar where mild proposal language would be more calibrated. None are quantitative or empirical, so severity is INFO rather than WARN.
