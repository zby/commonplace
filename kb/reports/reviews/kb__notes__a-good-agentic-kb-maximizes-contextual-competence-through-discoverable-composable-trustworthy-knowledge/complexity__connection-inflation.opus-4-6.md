warn

The note has 16 Relevant Notes entries. Several restate connections made explicitly in the body with inline links:

**Inflated entries:**
- `claw-learning-loops-must-improve-action-capacity-not-just-retrieval` — linked inline in paragraph 1 with "KB learning is broader than retrieval... names this directly." Footer adds nothing the body doesn't say.
- `first-principles-reasoning-selects-for-explanatory-reach-over-adaptive-fit` — linked inline in the Reach section ("David Deutsch distinguishes..."). Footer is redundant.
- `constraining-and-distillation-both-trade-generality-for-reliability-speed-and-cost` — linked inline twice in the operations section. Footer is redundant.
- `programming-patterns-get-a-fast-pass-but-other-borrowed-ideas-must-earn-first-principles-support` — linked inline in the Reach section with the same framing ("first-principles filter is a filter that selects for reach"). Footer is redundant.
- `sift-kg` and `siftly` — each has a dedicated sub-section explaining the contrast in full. Footer entries add no navigational value beyond those paragraphs.

**Justified entries:**
- `constraining`, `distillation`, `discovery-is-seeing-the-particular-as-an-instance-of-the-general` — pointer to definitions, useful for agents loading just the footer.
- `learning-is-not-only-about-generality` — footer adds "Simon's definition" attribution not explicit in the body paragraph.
- `Agentic Note-Taking 23: Notes Without Reasons` — the body never references this source by name; footer adds genuine context.
- `a-knowledge-base-should-support-fluid-resolution-switching`, `title-as-claim-enables-traversal-as-reasoning` — relationship labels add a framing angle not stated explicitly in the inline use.

Six entries (claw-learning-loops, first-principles-reasoning, constraining-and-distillation-trade, programming-patterns-fast-pass, sift-kg, siftly) are inflation candidates. Trimming to justified pointers and adding the inflated ones as inline prose would reduce noise without losing connections.
