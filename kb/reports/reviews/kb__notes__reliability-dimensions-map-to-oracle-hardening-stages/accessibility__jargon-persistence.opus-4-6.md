"oracle" / "hard oracle" / "soft oracle" / "no oracle" — introduced in the opening sentence via a link to oracle-strength-spectrum ("The [oracle-strength spectrum](./oracle-strength-spectrum.md) describes a gradient from hard oracles (cheap deterministic checks) to no oracle (vibes)"). The parenthetical glosses ("cheap deterministic checks" for hard oracle, "vibes" for no oracle) provide brief inline definition. These terms then recur throughout the table and body: "interactive oracle," "converts interactive oracle to hard oracle via repetition," "soft oracle ('it usually works')," "hard oracle ('it works under these perturbations')," "confidence score *is* a soft oracle," "hard oracle by design." The table entries reuse the terms without re-glossing. By the Connection to spec mining and Predictability gap sections, the terms are used without any context reminder. Given that the opening sentence provides a gloss and the note is short, the persistence burden is not severe. The table immediately reuses the terms within the same page. This is a borderline pass; the opening gloss is present and the note is short enough that scrolling is not required.
