pass — The title makes a contestable comparative claim. An informed reader could reasonably argue the opposite: that a stale index is still better than nothing because it gives partial coverage and can be updated, while no index forces every agent to search from scratch. The note earns the claim by identifying the specific asymmetry — a stale index suppresses fallback search by satisfying the navigation need, whereas no index degrades gracefully to search — which is the non-obvious mechanism that makes the comparison come out the way it does.
