## prose/source-residue

**Result: PASS**

The note's title and opening claim generality across "agentic systems" — LLM-based systems that interpret natural-language specifications. The body develops a framework (two phenomena, projection model, boundaries, constraining/relaxing) at that level of generality.

Concrete examples are drawn from the coding/programming domain:

- "Refactor for Readability" (line 43) — explicitly introduced as "Example:" with its own subsection heading.
- "file-renaming agent" / `sanitize_filename()` (lines 112–113) — explicitly introduced as "Example:" within the progressive constraining discussion.
- "LLM coding assistant" (line 44) — frames the refactoring example.

All domain-specific material is properly scoped as illustrative examples. The framework vocabulary itself (spec, projection, boundary, constraining, relaxing) is domain-neutral. No programming-specific terminology has leaked into the general framework claims.

The note does not overcommit to the coding domain despite drawing its examples from it.
