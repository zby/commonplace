## prose/unbridged-cross-domain

**Result: PASS**

Two external sources are cited:

1. **Ma et al. (2026) — Prompt Stability in Code LLMs**: The source studies emotion/personality prompt variations in code LLMs. The note cites it as evidence for the two-phenomena separation in LLM-based systems generally. This is not a cross-domain transfer — code LLMs are a subset of LLMs, so findings about code LLMs are direct evidence (a specific instance) for the general class. The note's relationship annotation correctly identifies the mechanism: "emotion/personality prompt variations change code output while holding task spec constant, isolating underspecification (which interpretation?) from indeterminism (which run?)."

2. **ABC: Agent Behavioral Contracts**: The source is about formal specification for agent systems. The note's domain is also agent systems. No domain crossing — the relationship annotation ("contracts resolve semantic underspecification with formal YAML DSL") identifies the shared mechanism.

The note's own examples (refactoring, file-renaming) are from the coding domain applied to an agentic-systems argument, but these are the note's own illustrative constructions, not cited evidence. No bridge is needed for self-constructed examples.

No unbridged cross-domain transfers found.
