## prose/anthropomorphic-framing

**Result: WARN**

Most LLM-referencing language in the note uses standard metonymy for software systems ("the LLM performs a projection," "the code treats the LLM's output as a concrete value," "the agent still handles ambiguous cases"). These are conventional technical shorthand.

One instance crosses into anthropomorphism:

- **"the LLM figures out how to do it"** (Relaxing as extension, line 87): "Figures out" implies cognitive problem-solving. A more precise alternative: "the LLM resolves it" or "the LLM produces an implementation."

The repeated use of **"picks"** ("The LLM picks one," "the projection picks an interpretation," "the LLM still picks one interpretation") is borderline — it attributes selective agency. However, "picks" is widely used as standard shorthand for algorithmic selection in ML contexts and the note is explicitly about the interpretation process, so flagging it as anthropomorphism would be overzealous.

**"choosing an interpretation you didn't intend"** (Debugging section) — same pattern as "picks." Standard metonymy in context.

One instance to fix; the rest are conventional.
