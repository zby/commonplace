## Result: WARN

One instance of restatement found.

### "Every codification is a bet" — second paragraph restates the regime distinction

The first paragraph ("Codification extracts a regularity and commits it to code") is a one-sentence transition that reframes codification in bet terms — this is fine.

But the second paragraph restates the arithmetic/vision-feature distinction:

> "In the arithmetic regime, the bet is safe: the spec IS the problem, and the solution is algorithmically determined. In the vision-feature regime, the bet is risky: the spec is a theory, and scale may reveal a better path."

Compare with the previous section's bullet points:

> - **Arithmetic-regime decompositions**: the spec fully captures the subproblem, so codification is pure gain...
> - **Vision-feature decompositions**: the spec is a plausible theory, so codification involves the real trade-off...

Both passages cover the same ground: arithmetic = spec is the problem = safe; vision-feature = spec is a theory = risky. The second section reframes in "bet" language, but the content is the same distinction. The third sentence ("Most practical codification happens in the blurry zone between them") does advance to new territory.

**Severity: WARN** — both passages fill the same argumentative role (establishing what happens in each regime). The restatement could be compressed to a brief callback: "Since the bet's safety depends on which regime you're in (§1), and most codification happens in the blurry zone between them..."
