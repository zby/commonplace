"select/call loop" — introduced with a code block and extensive explanation in "The select/call loop" section. Used many times thereafter: "the abstract select/call loop," "select/call program," "the select/call loop's decomposition." Each later use is in close proximity to or directly follows the model section; the term is a formal label for the loop defined in code. No issue.

"clean model" — introduced in the opening ("a degraded variant of the clean model") and defined implicitly as the symbolic scheduler + bounded LLM calls architecture. Later uses: "the clean model," "still the clean model," "clean-model instantiation." The term is consistently used as shorthand for the architecture described in "The model" section. However, "clean model" appears in the very first paragraph before the model is formally defined — a reader must continue reading before the term resolves. This is a mild ordering issue rather than a persistence issue.

"bounded call" / "bounded LLM call" / "bounded clean context windows" — these compound terms are used throughout. "Bounded" is the key modifier meaning context-limited. The note introduces it in the opening ("bounded LLM calls") and uses it consistently. The meaning remains clear.

"K" — this is notation handled under the notation-opacity gate.

pass (no jargon-persistence issues; notation is covered separately)
