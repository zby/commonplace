pass — The body directly establishes the title's sequencing claim. It introduces the three-regime model (flat prompting, specification-level separation, architectural separation), demonstrates that OpenProse recovers scoping gains (naming control flow, externalizing state) while leaving the error-correction mechanism (hard oracles, medium boundary) unrecovered, and closes by anchoring the claim against the broader scheduler-LLM separation argument. There is no drift; the body supports exactly what the title promises.
