"Clean model" appears in "That is not the clean model" without any definition or gloss. This is KB-specific vocabulary (from the scheduler-LLM separation note) that the reader cannot interpret without following the link. Either gloss it inline — e.g., "That is not the clean model (full symbolic scheduling with the LLM restricted to semantic steps)" — or replace with a plain description.

"Hard oracle" and "soft oracle" appear five times across the note — "hard-oracle substrate," "soft-oracle checkpoints," "hard oracles to enforce," "hard reliability," "soft-oracle operations" — but are never defined. The first use ("explicit soft-oracle checkpoints") assumes the reader already knows the oracle-strength spectrum. A single grounding sentence near first use would anchor all subsequent mentions: e.g., "soft-oracle checkpoints — checks that rely on LLM judgment rather than deterministic verification."
