**Pass.** The note has no named sections other than the Relevant Notes footer, and it carries two distinct non-obvious claims: (1) specification-level separation (naming control flow, externalizing state, marking discretionary holes) recovers scoping and resumability benefits before it recovers hard reliability, creating a genuine intermediate regime rather than a binary choice; and (2) this ordering is specific to specification-level approaches — tool-use frameworks with typed parameters take a different path (mini-codification at the boundary) that does not pass through this intermediate regime. The note also establishes a three-level spectrum (flat prompting / specification-level separation / architectural separation) to locate these claims. The absence of heavy section structure matches the note's analytical density: it is a single sustained argument, not a multi-claim survey.
