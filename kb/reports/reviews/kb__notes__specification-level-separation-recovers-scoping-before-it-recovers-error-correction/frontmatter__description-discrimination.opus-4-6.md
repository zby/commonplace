pass — The description adds concrete scope and mechanism beyond the title: it names OpenProse-like DSLs as the subject class, identifies what the intermediate regime does (expose control flow and discretion boundaries), and specifies what remains on the LLM substrate (scheduling and validation). It also names the design space being navigated (between flat prompting and symbolic scheduling), giving a retrieval reader enough context to distinguish this from a general note about LLM architectures or codification.
