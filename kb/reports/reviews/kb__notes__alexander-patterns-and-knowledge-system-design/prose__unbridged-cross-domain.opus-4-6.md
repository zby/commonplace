## prose/unbridged-cross-domain

**Result: WARN — two partially unbridged transfers**

The entire note is a cross-domain mapping (architecture → knowledge systems), so this gate is the most load-bearing check. The three sections vary in bridging quality.

### Concrete section — adequately bridged

The mapping from Alexander's pattern structure (Context/Problem/Forces/Solution/Consequences) to document types with required sections is bridged by structural isomorphism: both are contracts with required fields that enable traversal. The shared mechanism is clear enough that the transfer holds.

### Structural section — one unbridged identity claim

The mapping from generative process to codification trajectory is stated as "This maps to our codification trajectory" — a direct identity claim. The shared mechanism (incremental formalization driven by observation rather than upfront planning) is inferable but not stated. More problematic: "The methodology enforcement gradient (instruction → skill → hook → script) is a generative process" claims identity with Alexander's concept without bridging. Alexander's "generative process" produces "wholeness" through steps that respond to the *current state of the emerging whole*. The enforcement gradient is a series of automation levels ordered by rigidity. The note should state what mechanism makes this a generative process in Alexander's sense rather than merely an incremental sequence.

### Vague section — hedged but then applied without bridge

"Notes in a knowledge graph might work the same way" appropriately hedges. But subsequent sentences proceed as if the analogy holds: "Links create mutual reinforcement," "the graph has 'life'," and link relationship semantics "describe *how* centers strengthen each other." Alexander's "center" is a rich concept with spatial, geometric, and recursive properties. The note uses it as equivalent to "a note in a graph" without stating what is preserved and what is lost in that transfer. The hedge at the start does not substitute for a bridging mechanism.

### Recommendation

For the structural section: state the shared mechanism explicitly — what makes the enforcement gradient a generative process rather than just an incremental sequence. For the vague section: either strengthen the bridge (what property of Alexander's centers is preserved in notes-as-centers) or keep the entire section in analogy mode rather than switching to application mode after the initial hedge.
