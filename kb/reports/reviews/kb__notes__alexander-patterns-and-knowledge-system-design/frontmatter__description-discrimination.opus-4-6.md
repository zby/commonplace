pass. The description adds scope and critical context that the topical title cannot: it names the three specific levels (document contracts, generative-process argument, "centers" level), flags which connection is strong (concrete) vs weak (vague), and specifies the implication of the generative-process argument (incremental codification over upfront type systems). An agent searching for "why resist premature formalisation" would find this note via the description but not the title alone. Clearly additive.
