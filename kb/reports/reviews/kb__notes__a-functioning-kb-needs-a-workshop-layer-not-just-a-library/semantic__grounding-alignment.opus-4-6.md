## PASS

Checked 5 links for attribution accuracy:

1. **[current type system](./document-classification.md)** — the note claims the type system "is oriented toward permanence." The target (document-classification) describes the base types (text, note, structured-claim, etc.) without explicitly discussing permanence, but the types listed are library-oriented by structure. The permanence framing is the workshop note's analytical interpretation applied to the classification note's content — the link is used as the system being analyzed, not as a source supporting the permanence claim. Valid use.

2. **[spec mining](../notes/spec-mining-as-codification.md)** — the note says "At the deterministic end, this is spec mining: extracting rules and verifiers from observed workshop behavior." The target defines spec mining as "extracting deterministic verifiers from observed stochastic behavior." The workshop note narrows "stochastic behavior" to "workshop behavior" — a scope application rather than misrepresentation. The "at the deterministic end" qualifier correctly positions spec mining as one end of the extraction bridge spectrum. Valid.

3. **[constraining](./definitions/constraining.md)** — referenced in the Relevant Notes footer as "extraction bridges are constraining." The constraining definition centers on "narrowing the interpretation space." Extraction bridges produce new artifacts from workshop processes — this is closer to creation/commitment than to constraining as defined. However, the constraining note does say "At the heavy end, you commit to a single interpretation: storing a specific LLM output or extracting a deterministic function collapses the space to a point." Extraction bridges that commit to library artifacts do collapse possibilities. The connection is plausible but not airtight.

4. **[instructions are typed callables](../notes/instructions-are-typed-callables.md)** — the note says "Skills already have implicit type signatures (`/ingest: source → source-review`)." The target confirms: skills are typed callables that "accept document types as input and produce types as output." Attribution accurate.

5. **[claw learning loops](./claw-learning-loops-must-improve-action-capacity-not-just-retrieval.md)** — the note claims it "argues the KB needs action-oriented knowledge types (preferences, procedures, precedents, voice)." The target argues for improving action capacity, not just retrieval. The specific list "(preferences, procedures, precedents, voice)" may be this note's extrapolation rather than a direct enumeration from the target — but the general claim about action-oriented knowledge is supported.

No WARN-level scope overreach or invalid inference found.
