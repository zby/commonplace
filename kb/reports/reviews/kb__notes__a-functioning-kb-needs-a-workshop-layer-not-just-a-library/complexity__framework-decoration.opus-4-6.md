## PASS

The library/workshop comparison table is the main framework. One-sentence replacement attempt: "Library documents accumulate value and age slowly; workshop documents consume value, have state machines, and expire."

This loses the four other dimensions (state type, relationship directionality, success state, end state) that make the distinction operationally testable. The table communicates more than a sentence would — a reader can check whether a new document type is library or workshop by testing it against multiple dimensions. The framework is load-bearing.
