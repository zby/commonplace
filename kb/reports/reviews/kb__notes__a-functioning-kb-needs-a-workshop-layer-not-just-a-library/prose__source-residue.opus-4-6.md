## PASS

The note's claimed generality is about knowledge bases needing workshop layers. The body draws heavily on the existing task system as an example, but explicitly frames tasks as one instance ("Tasks aren't the only workshop documents") and lists six other temporal document types. The task-system vocabulary doesn't overcommit the note to that source domain — it's framed as the existing prototype, not the whole story.
