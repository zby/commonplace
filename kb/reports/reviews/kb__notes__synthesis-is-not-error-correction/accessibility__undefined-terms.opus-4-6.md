"decorrelation" is used in the MAKER section without a prior definition in this note: "red-flagging (discarding responses >700 tokens or with format violations) removes the output class most likely to carry correlated errors; temperature variation adds sampling diversity. These measures push toward the i.i.d. error assumption that makes voting's statistical guarantees hold." The concept is described by the surrounding detail, but the term "decorrelation" itself is first used without a gloss in the preceding sentence: "Decorrelation — red-flagging..." The structure of that bullet provides enough context to infer meaning, so this is borderline. Acceptable as written.

"first-to-ahead-by-k voting" is used in "MAKER uses first-to-ahead-by-k voting on maximally decomposed micro-steps" without any inline explanation of what "ahead-by-k" means mechanically. A reader who hasn't read the MAKER source cannot understand what this voting protocol does. Recommend adding a brief gloss: e.g. "a voting scheme that accepts the first answer that appears k times in a row" (or whatever the actual definition is — the author should supply this from the source).

"soft oracle" is used in "an orchestrator reviews sub-agent outputs — a soft oracle, partially filtering errors" — the inline gloss "partially filtering errors" gives enough context to understand the degree of filtering, but "soft oracle" as a term is defined only in the linked oracle-strength-spectrum note, not here. This is covered by the exception for terms defined in `kb/notes/definitions/` when the note links to the definition — however the link here is in the Relevant Notes section rather than at the point of use. This is a minor issue; the gloss "partially filtering errors" provides enough context inline.