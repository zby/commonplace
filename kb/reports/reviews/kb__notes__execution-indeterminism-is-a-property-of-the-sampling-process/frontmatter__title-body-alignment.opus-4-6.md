**pass** The body supports the title's attribution claim and then extends it usefully: it explains why indeterminism is "engineering noise" (variation in execution, not interpretation), why temperature=0 doesn't fix underspecification, and why indeterminism obscures the deeper issue. The body's scope slightly exceeds the title — it also argues that indeterminism misleads practitioners toward the wrong remedies — but this extends rather than contradicts the title.
