The note cites five KB notes and one distilled artifact. Central claims traced below.

---

**Claim: context is the scarce resource making unbounded reading-together infeasible**

Cited to [context-efficiency-is-the-central-design-concern-in-agent-systems.md]. Consistent with the KB's foundational treatment. ✓

**Claim: a note missing from its area is invisible to orientation and comparative reading**

Cited to [stale-indexes-are-worse-than-no-indexes.md]. The inference is direct: if the area index doesn't list the note, neither operation will include it. ✓

**Claim: the ~40 note split threshold isn't arbitrary — it's the approximate context limit**

Cited to [WRITING.md]. The note provides a mechanistic grounding (context budget alongside instructions and reasoning space). INFO — the "~40" threshold is presented as following from context limits, but the precise number likely depends on note length, model context window, and how much room is needed for reasoning. The note says "approximate" which is appropriately hedged, but the grounding (context limits → ~40) could be tested empirically.

**Claim: cross-domain connections justify /connect with corpus expansion rather than routine area loading**

Cited to a deep-search note. The reasoning — cross-domain connections are rare enough that a different, more expensive operation is justified — is the note's own contribution. The inference is valid: if most note-pairs in a cross-domain set yield nothing, the expected yield per context-unit is low. ✓

**Claim: "areas are one resolution level" in the resolution-switching framework**

Cited to [a-knowledge-base-should-support-fluid-resolution-switching.md]. This is a brief connection, not a strong grounding claim. ✓

**Self-grounded conventions**

The conventions (precise tagging, no dual parent-child tagging, multi-area for independent dimensions) are grounded in the note's own operations analysis. They follow logically from the yield-per-context-unit framework. ✓

---

No WARN. One INFO on the ~40 threshold's empirical grounding.
