"OpenViking" is introduced at first mention as "OpenViking's `.abstract.md` is a pointer in their virtual filesystem" and used throughout the note (comparison tables, design implications) without ever saying what it is. The reader can infer from context that it is some kind of knowledge management system with a tiered abstraction structure, but the note never identifies it (e.g. as an agent knowledge framework, a research project, etc.). The link in the Relevant Notes section does not substitute for inline identification. Suggested fix: add a brief identification on first use, e.g. "OpenViking (an agent knowledge-management framework) uses `.abstract.md` as a pointer in their virtual filesystem."