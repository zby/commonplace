## prose/bridge-paragraph-duplication

**Result: PASS**

Checked each section boundary:

- **The gap → Concrete use cases**: The gap ends with "The question is whether inbound connections should be visible at reading time, and if so, how." This frames the question without enumerating the four use cases that follow.
- **Concrete use cases → Non-use-cases**: No bridge paragraph; the last use case ends with its own content.
- **Non-use-cases → Design options**: No bridge paragraph.
- **Design options intro**: "The use cases above all need the same underlying data — an inverted link index. They differ in whether that data should be visible in the note, computed on demand, or both." This adds new analytical content (shared data structure, visibility as the differentiator) rather than previewing the four options.
- **Design options → Trade-offs**: No bridge paragraph.

No duplication found. Transitions are concise and additive.
