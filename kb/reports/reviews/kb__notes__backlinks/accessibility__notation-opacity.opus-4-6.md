The note contains one inline code snippet used as a search command example: `` `rg 'note-title\.md' --glob '*.md'` ``. This is a shell command, not a symbolic variable from another note, and its meaning is inferrable from context ("Grep-based discovery exists"). No notation-opacity issue.

pass
