The note introduces several KB-internal terms and tracks them throughout.

**"enforcement gradient"** — introduced in the opening sentence with a link, then used in "The enforcement gradient table has columns" (para 2), "extends the enforcement gradient with a recovery column" (para 3), and "Recovery automation tracks oracle strength" (para 3). Three uses after definition; each use is in a context where it still makes sense without scrolling back ("the table," "column added to it"). Borderline but acceptable.

**"oracle strength"** — introduced via a link in the opening sentence ("whether the agent actually applies the fix remains probabilistic"), then used as a section header and repeatedly in the oracle section. The section header "Oracle strength constrains recovery automation" restates the concept plainly before the term is pressed hard, so readers can re-anchor. Acceptable.

**"hard oracle" / "soft oracle" / "interactive oracle" / "no oracle"** — introduced as a four-way taxonomy in the oracle section and each used once in that section. No persistence issue.

**"corrective action" / "fallback chain" / "escalation"** — introduced in the numbered list in "Recovery as a typed strategy" and then used in the table. The table appearance is immediately below the definition, so no scrollback required.

**"ABC framework"** — introduced once with a link in "Recovery as a typed strategy" and referenced again at the end of the oracle section ("ABC's Drift Bounds Theorem"). The second use appears several paragraphs later; a reader who skimmed might not remember "ABC" stands for "Agent Behavioral Contracts." However, the term is only used twice after the definition, so this is at the edge of the rule threshold but not a clear failure.

**"enforcement gradient" in Open Questions** — "Should the enforcement gradient table in [methodology-enforcement-is-constraining] gain a recovery column" — the fifth use of the term. By this point the term has been used heavily and the meaning is clear in context. Acceptable.

No clear jargon-persistence failures.

pass
