Pass. The recovery table in "Recovery as a typed strategy" earns its place: it maps five enforcement layers against detection and recovery columns, making visible which layers have structured recovery and which do not. This structural contrast — that detection is reliable at most layers while recovery is unstructured — is the core argument, and the table communicates it more efficiently than prose would. Deleting it and replacing it with a sentence would lose the per-layer breakdown that grounds the oracle-strength discussion that follows.
