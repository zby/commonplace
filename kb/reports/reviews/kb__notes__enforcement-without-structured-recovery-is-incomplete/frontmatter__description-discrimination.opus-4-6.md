pass — The description adds substantial mechanism and context beyond the title: it names the specific gap (the enforcement gradient has no recovery column), supplies the three-step recovery taxonomy (corrective → fallback → escalation), and names the determining constraint (oracle strength). An agent searching for notes about enforcement would immediately see this note addresses the post-detection phase rather than the detection and blocking phases.
