**"enforcement gradient"** — introduced in the first sentence as a link to another note: "The [enforcement gradient](./methodology-enforcement-is-constraining.md) captures two dimensions of methodology enforcement — trigger reliability and response determinism." The inline description ("captures two dimensions of methodology enforcement — trigger reliability and response determinism") gives enough context to understand the term without following the link. Acceptable.

**"blocking hook" / "warning hook"** — first appear in "A blocking hook rejects the operation; a warning hook outputs a signal." Both terms are defined inline by their effects. Acceptable.

**"oracle"** — used in "whether the agent actually applies the fix remains probabilistic. Detection and blocking are systematic; recovery is ad hoc." Then later "oracle strength" appears as a section. The concept of an oracle (a verification mechanism) is introduced through the oracle strength section: "Hard oracle (deterministic verification) — corrective action can be fully automated. The system knows what 'correct' looks like." This gives sufficient inline context before the term is used heavily. Acceptable.

**"Drift Bounds Theorem"** — appears in "ABC's Drift Bounds Theorem formalises the cost of missing recovery." The theorem is named but not described beyond "formalises the cost of missing recovery." A reader encountering this for the first time has no way to understand what kind of formal result this is — a bound, a convergence theorem, a measure-theoretic claim? The name "Drift Bounds Theorem" is not self-describing enough, and the note does not paraphrase what the theorem states before citing it. The formula D*=α/γ appears right after, but the theorem name itself arrives without any contextualizing clause.

Finding: "ABC's Drift Bounds Theorem formalises the cost of missing recovery" — "Drift Bounds Theorem" is used as if the reader already knows it. A brief gloss such as "ABC's Drift Bounds Theorem — which gives the long-run drift level as a function of detection and recovery rates — formalises..." would make this readable in isolation.
