# {{project_name}} Knowledge Base

<!-- One sentence: what this KB is and who it serves. -->

## KB Goals

### Purpose

<!--
What decisions or actions does this KB support? Who uses it?
Example: "This KB supports the API team in making design decisions about the payment service. It accumulates architectural reasoning, failure analysis, and integration patterns so that agents and developers can make informed decisions without re-deriving context from code and Slack history."
-->

### Domain

<!--
What is this KB about? Draw a scope boundary.
Be specific enough that an agent can decide "does this belong?" without asking.
Example: "Payment service architecture, integration patterns, and failure modes. Adjacent systems (auth, billing) are in scope only where they interact with payments. General distributed systems theory is in scope when it directly informs a specific design decision. Business rules and product requirements are out of scope — they live in the product wiki."
-->

### Include

<!--
What types of knowledge belong here? Be concrete.
Examples: design decisions, failure analysis, integration patterns, operational procedures, team conventions.
-->

-
-
-

### Exclude

<!--
What doesn't belong despite seeming relevant? This prevents scope creep.
Examples: raw logs without analysis, business rules (live in product wiki), general theory without specific application, code-level documentation (belongs in code comments).
-->

-
-
-

### Quality bar

<!--
When is a piece of knowledge worth a note vs. a log entry vs. nothing?
This complements each COLLECTION.md's universal criteria with domain-specific standards.
Example: "A design decision is worth a note when it affects more than one endpoint. Single-endpoint implementation details belong in code comments. Observations about failure patterns are worth a log entry on first occurrence and a note on third."
-->

## Vocabulary

This section declares the active vocabulary: terms with specific meanings throughout this KB.

<!--
Add project-specific terms only when the KB needs a stable operational meaning.
Prefer backing definition notes under kb/notes/definitions/ when a short gloss is not enough.

Example:
- **Service boundary** — the interface where one service's ownership ends and another begins. See `kb/notes/definitions/service-boundary.md`.
-->

## Using the KB

The knowledge base lives in `kb/`. Search it when the user asks about decisions, reasoning, design rationale, or project knowledge.

### Collection Routing

Collections are `kb/` subtrees with a local `COLLECTION.md`. Read the target collection's `COLLECTION.md` before writing or connecting artifacts there.

| Path | Role | Use when |
|---|---|---|
| `kb/notes/` | user theoretical register | Writing transferable claims, mechanisms, definitions, synthesis, and project-specific theory. |
| `kb/reference/` | user descriptive register | Describing the current system, architecture, interfaces, decisions, and operational surface. |
| `kb/instructions/` | user prescriptive register | Writing project-specific procedures, skills, operational rules, and how-to guidance. |
| `kb/sources/` | user source material | Storing external snapshots, ingests, and source reviews. |
| `kb/work/` | user workshop layer | Holding in-flight investigations, drafts, migration plans, and temporary work that should eventually close or promote durable artifacts. |
| `kb/commonplace/notes/` | shipped methodology library | Reading Commonplace theory and examples without mixing them into the user's own notes. |
| `kb/commonplace/reference/` | shipped Commonplace docs | Looking up Commonplace architecture, commands, type system, and ADRs. |
| `kb/commonplace/instructions/` | shipped Commonplace procedures | Reading framework procedures and promoted `cp-skill-*` skill bodies. |
| `kb/commonplace/` | namespace, not a collection | Locating shipped library collections; do not treat the namespace itself as governed content. |
| `kb/types/` | global type surface, not a collection | Looking up shared type specs used by both user and shipped collections. |

Resolve an artifact's collection by walking up to the nearest ancestor `COLLECTION.md`. A `COLLECTION.md` inside a non-collection namespace is an ordinary collection; introduce a `COLLECTION.md` inside another collection only deliberately.

```bash
# Find notes by description
rg "^description:" kb/notes/ kb/reference/ kb/instructions/ --glob "*.md"

# Find notes by type
rg "^type: note" kb/notes/ kb/reference/ kb/instructions/ --glob "*.md"

# Find notes by tag
rg "^tags:.*my-tag" kb/notes/ kb/reference/ kb/instructions/ --glob "*.md"
```

Key entry points:

- `kb/notes/index.md` — auto-generated directory listing (rebuild with `commonplace-generate-notes-index kb/notes`)
- `kb/reference/README.md` — current-state documentation for this system and links to its ADRs

<!-- Add curated tag indexes here as they emerge. Create a tag index when 5+ related notes accumulate without navigation structure. -->

### Skills

`commonplace-init` installs the `cp-skill-*` family (`cp-skill-write`, `cp-skill-validate`, `cp-skill-connect`, etc.) into `.claude/skills/` and `.agents/skills/`. The harness loads them automatically.

### Commands

The `llm-commonplace` package provides `commonplace-*` CLI commands for validation, indexing, snapshots, note operations, and the review system. For the full reference, read [kb/commonplace/reference/commands.md](./kb/commonplace/reference/commands.md).

For review work (single-note review, triage, ack, or sweep), read `kb/commonplace/instructions/REVIEW-SYSTEM.md`.
For fixing review warnings, read `kb/commonplace/instructions/FIX-SYSTEM.md`.

For writing conventions, read the target collection's `COLLECTION.md`; it is the local authoring and routing contract for that collection.
